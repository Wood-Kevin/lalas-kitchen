# Deferred Complexity

One line per deferred idea, with a short reason it was deferred. Parked is fine. Forgotten is not.

- Tuning-constant review — done: a full inventory of every hand-picked constant across the generator/difficulty ramp, cross-referenced for genuine inconsistencies. See `engine/DECISIONS.md`'s tuning-constant-review entry. Found and fixed two real gaps (a freshly-spread blocker losing its `specialOnly` flag when a sealed_jar-gated level's denial zone also spreads; clearance objectives never receiving breather relief on their actual workload, unlike collect/score), corrected two stale comments, and confirmed several existing claims by direct computation rather than trusting the prose. Everything else inventoried was already honestly disclosed as hand-picked-not-playtested at the point each feature was built — nothing further needed re-disclosing.
- Board topology (conveyors, portals, wraparound edges) — investigated, not built, no reasonable bounded scope found. See `engine/DECISIONS.md`'s board-topology entry: every real idea in this space requires a piece to cross a column boundary during gravity resolution, but `engine/matrix.ts`'s `calculateCascades` is structurally a per-column-only algorithm with zero cross-column data flow — this isn't a missing branch, it's a different algorithm shape. It's also the single most heavily-relied-upon piece of the engine (void-cell shapes, denial-zone spread, every special piece's refill, the dropdown mechanic, and blocker depth all sit on top of its per-column guarantee), so a structural rewrite risks regressing all of them at once in ways the current test suite — which asserts today's contract, not a hypothetical cross-column one — wouldn't necessarily catch. Revisit only as its own dedicated phase (redesign `calculateCascades` itself, verify in isolation, then layer a topology feature on top), not as an incremental addition.
- Blocker depth (`specialOnly` blocker, skin id `sealed_jar`) — built: a blocker variant that only takes adjacent damage from a special effect (sweep/blast/detonation/chain), never a plain ordinary match, gated to generated levels at or past level 12. See `engine/DECISIONS.md`'s blocker-depth entry and `docs/verification/blocker-depth/`. Deliberately narrow, deferred rather than built: (a) **no distinct visual overlay** beyond the blocker's own sprite — matches the precedent every other blocker variant (cling/dish_stack/pot_lid) and the dropdown piece already set (sprite-only, no dedicated overlay component); (b) **no dedicated tutorial** teaching "this blocker needs a special piece" — every other genuinely new mechanic in this project got a one-time tutorial card, but authoring an 8th was judged out of scope for this closing pass; a player currently has to infer the behavior from the blocker simply not responding; (c) **the gate level (12) and hit count (1) are hand-picked, not playtested** — whether sealed_jar should be tougher (2 hits) or gated earlier/later is a real, undecided tuning question. ~~(d) **no dedicated sprite art — the blocker rendered as the "SE" text-label placeholder**, disclosed only inline in `engine/DECISIONS.md`'s own blocker-depth entry and in `docs/verification/blocker-depth/`'s screenshots (item (a) above's "beyond the blocker's own sprite" phrasing was itself ambiguous about whether that sprite actually existed), but never actually itemized here alongside (a)–(c) the way the mechanic's other gaps were~~ — **fixed**: a real illustrated jar sprite (`skins/lalas-kitchen/sprites/sealed_jar.webp`) is now registered in `spriteRegistry.ts`, following the same filename-keyed pattern as the other three blockers. Surfaced by a real playtest report of a piece rendering as "SE" text; investigation confirmed it wasn't a regression — no sealed_jar sprite had ever been registered since the feature first shipped. Verified live: real generated `sealed_jar` blockers on generated level 20 now render the jar art, with zero "SE" text nodes remaining anywhere on the page — see `docs/verification/sealed-jar-sprite-art/`.

- Dropdown ingredients (escort mechanic) — built: a colorless `'dropdown'` piece type immune to every clearing effect, collected purely by reaching the bottom of its column via ordinary gravity, feeding a new `'escort'`-type `Objective`. See `engine/DECISIONS.md`'s dropdown-ingredients entry and `docs/verification/dropdown-escort-mechanic/` (which also documents a real crash this feature caught live in `components/Board.tsx`'s animation pipeline, since fixed). Deliberately narrow, deferred rather than built: (a) **`buildGeneratedLevelConfig` never produces `dropdownPositions`/an `'escort'` objective** — hand-built content only (one level, "Delivery Day"), matching the same incremental pattern score/clearance objectives both started with before their own later generator-integration sessions; (b) **no dedicated fall/collection animation** — a dropdown piece uses the exact same generic cascade-fall visual every other piece already gets, no distinct identity (unlike the striped/color-bomb/area-bomb specials, which each got their own animation-identity session); (c) **the two `dropdownPositions` chosen for "Delivery Day" (both top-row) are a hand-picked, not playtested, starting difficulty** — how many dropdown pieces, where they start, and how that should scale with a level's move budget are all real, undecided tuning questions. ~~(d) **no dedicated sprite art — the piece rendered as the "DR" text-label placeholder**, disclosed only inline in `engine/DECISIONS.md`'s own dropdown-ingredients entry and in `docs/verification/dropdown-escort-mechanic/`'s screenshots, but never actually itemized here alongside (a)–(c) the way the mechanic's other gaps were~~ — **fixed**: a real illustrated basket sprite (`skins/lalas-kitchen/sprites/dropdown.webp`) is now registered in `spriteRegistry.ts`, following the exact `area_bomb.webp` one-line pattern. Surfaced by a real playtest report of a piece rendering as "DR" text; investigation confirmed it wasn't a regression — no dropdown sprite had ever been registered since the mechanic first shipped. Verified live: a real dropdown tile on "Delivery Day" now renders the basket art, with zero "DR" text nodes remaining anywhere on the page — see `docs/verification/dropdown-sprite-art/`.

- `isValidSaveData` (`engine/gameState.ts`) never actually validates `consecutiveLosses`'s type if present — noticed while adding `lastCrash`'s own validation alongside it, not fixed (out of scope for that task, a pre-existing gap unrelated to crash telemetry). A save with a malformed `consecutiveLosses` (e.g. a string) would currently pass schema validation and only misbehave later wherever it's read as a number. Low real risk (nothing writes it except `buildSaveData`, always a real number) — worth a one-line fix (`if (value.consecutiveLosses !== undefined && typeof value.consecutiveLosses !== 'number') return false;`) next time this function is touched.
- Save-data backup/export beyond the OS-level default — investigated (see `engine/DECISIONS.md`'s save-backup entry), not built as a new feature. Android already gets Auto Backup to Google Drive for free (`android:allowBackup="true"`, Expo's own prebuild default, unchanged). iOS did NOT — `@react-native-async-storage/async-storage` excludes its own storage from iCloud backup by default — fixed with a one-line `app.json` config flip (`ios.infoPlist.RCTAsyncStorageExcludeFromBackup: false`), not new infrastructure; unconfirmed on a real device (no iOS device/build exists in this environment, the standing disclosed gap every native-only feature here carries). Web (`localStorage`) has no OS-level backup at all and was deliberately left as an accepted gap: no account/backend system exists to sync against, and one wasn't built for a single-player personal project. A manual export/import (copy a backup code, or a real file via a share sheet) was considered and would close it, but adds real UI/dependency surface for a rare need — revisit only if losing web progress ever becomes a real, repeated pain point.

- Sound/haptics stub layer: the infrastructure (services/soundService.ts, services/hapticsService.ts, SaveData flags, Home's toggle, event wiring in Board.tsx) is **built** — see engine/DECISIONS.md's sound/haptics stub-layer entry. ~~(a) a real audio backend~~ — **resolved**: `services/expoAudioSoundService.ts` (real `expo-audio` playback, chosen over the deprecated-in-SDK-53/removed-in-SDK-55 `expo-av`) is now the real `SoundService`, with three procedurally synthesized WAV files (`scripts/generate-sound-assets.js`, since no audio tool or licensed sound-asset access existed in the build environment) registered in `skins/lalas-kitchen/soundRegistry.ts` — see engine/DECISIONS.md's real-audio-backend entry and `docs/verification/real-audio-backend/`. Still genuinely open from that work, disclosed rather than assumed done: only `match.wav` was live-triggered end-to-end (cascade/win share the identical code path but weren't independently confirmed live); no native device/simulator was available to confirm the real native `AudioPlayer` backend; and no human has listened to the three tones to confirm they read as calm/pleasant, only that real playback is invoked without error. (b) **no distinct `combo_streak` sound** — the original ask scoped this to match/cascade/win only; a fourth `SoundEffectId` plus a call site in `Board.tsx`'s `attemptSwap` (alongside the existing `hasCombo` computation) is a small, obvious follow-up if wanted; (c) **no automated `npx expo install`** — this environment's npm config rejects `expo install`'s internal `--allow-scripts` flag for project-scoped installs, so `expo-haptics` and (this session) `expo-audio` were installed via a manually-pinned `npm install <pkg>@<bundled-version>` instead (matching `expo/bundledNativeModules.json`'s own pin) — revisit if this environment constraint changes.

- Real device build crash (`NoClassDefFoundError` in `expo.modules.asset.AssetModule`) caused by `expo-audio`'s unconstrained `"expo-asset": "*"` peer dependency pulling in a second, SDK-incompatible top-level `expo-asset@57.0.3` alongside the correct nested `~12.0.13` — **resolved**: `expo-asset` pinned as an explicit top-level dependency at `~12.0.13` in `package.json`, deduping npm onto the one correct copy — see `engine/DECISIONS.md`'s "Fixed: a real device build crash..." entry. Still open, disclosed rather than assumed fixed: no Android SDK/JDK/device exists in this environment, so the actual native build launching cleanly on a real device has not been directly re-confirmed — only the dependency-resolution mechanism (`expo-modules-autolinking resolve -p android` now correctly resolving `expo-asset` to `12.0.13`) was verified. Revisit once a real device/build is available to close this out for good.

- ~~Row clearers~~ — resolved: the striped piece (a run of exactly 4 spawns a `type: 'striped'` row/column clearer; matching it sweeps that line) is built — see engine/DECISIONS.md's striped-piece entry. Remaining special-piece scope still deferred, deliberately, so the special-piece tiers stay bounded and correct: ~~(a) sweep/blast chaining~~ — **resolved**: a striped piece, color bomb, or area bomb caught in another's line sweep / detonation / 3×3 blast (or in a combo's clear, or an in-match striped sweep) now fires its own effect too, recursively to fixpoint — see engine/DECISIONS.md's special-piece-chaining entry and `docs/verification/special-piece-chaining/`. Built as one shared `expandChainClears` (engine/gameState.ts) wired into both `resolveClearSet` and `resolveMatchEffects`; a caught color bomb (no swap partner) detonates the board's most-common color. One presentation nicety from that work is deferred: the **per-link animation flash** — the engine folds the whole chain into a single committed clear set (one move, one beat), so there's no intermediate frame showing each link fire in turn; the settled result is identical, only the mid-chain staging is missing (same shape as the supercombo convert-to-striped flash). (b) **L/T-shape triggers** — overlapping straight runs are each handled independently (a 4-run's anchor cell wins if another run also wanted to clear it), with no combined/wrapped special for the intersection; a color bomb only spawns from a *straight* 5, and an **area bomb** only spawns from a *pure 2×2 square* (four cells no run touches), never from an L/T intersection of a run and a square meeting at shared cells — the built area-bomb detection deliberately stands the square down whenever its cells overlap a run, leaving the run logic to own them (see engine/DECISIONS.md's area-bomb entry). A genuine two-arm crossing (both arms exactly 3) is now a separate, additive trigger — see engine/DECISIONS.md's crossing-run entry — but an L/T intersection worth a striped piece or color bomb (i.e. one involving a 4- or 5-long arm) remains fully deferred exactly as described above: that intersection stands down at the shared cell and the existing run logic (striped/color-bomb spawn) fires unaffected. ~~(c) bomb + striped combos~~ — **resolved**: swapping a striped piece with a color bomb now converts every piece of the striped piece's matchType into a striped piece and fires them all (the super-combo), and swapping two striped pieces clears a full cross — see engine/DECISIONS.md's special-piece combos entry and `docs/verification/special-piece-combos/`. ~~One presentation nicety from that work was deferred: the convert-to-striped flash~~ — **resolved**: the supercombo now plays a real two-beat presentation identity (a brief "converting" flicker on every converted piece, then a synchronized pop-and-shrink together, rather than the engine's single computed clear rendering as one flat instant) — see engine/DECISIONS.md's special-effect-identity entry and `docs/verification/special-effect-identity/`. This is presentation only; the engine still computes the settled clear directly, with no intermediate "everything is now visibly a striped piece" board state — only the animation now shows two beats where the underlying data is one. (Blocker clearing was built earlier — see engine/DECISIONS.md's Phase 6 section.)
- ~~5+-match specials (color bomb)~~ — resolved: a run of **exactly 5** now spawns a `type: 'color_bomb'` piece; swapping it with any piece detonates every piece on the board sharing that piece's matchType (swapping two bombs clears the whole board). It activates on the swap itself, a genuinely different mechanism from the striped piece's match-triggered activation — see engine/DECISIONS.md's color-bomb entry, and `docs/verification/color-bomb/` for the live capture. A run of **6+** still clears normally with no larger special; the L/T-shape *trigger* for a color bomb (intersecting runs meeting at a shared point, rather than one straight run of length 5) is deferred alongside the L/T combos in the entry above (the newly-built crossing-run trigger only ever spawns an area bomb from two exactly-3 arms; it never upgrades a 5-long arm's own color-bomb spawn).
- ~~2×2-square specials (area bomb)~~ — resolved: a **2×2 square** of four same-type pieces spawns a `type: 'area_bomb'` piece; **swapping it with any ordinary piece** fires the full **3×3** block around it. It's now a **colorless, swap-activated** special (drops its matchType), the same camp as the color bomb — this **reverses** its original passive/colored design (matching a colored bomb into a run), because the single universal `area_bomb.webp` sprite gave a player no way to see which color a passive bomb was; making it colorless makes the ambiguity moot. See engine/DECISIONS.md's area-bomb reversal entry and `docs/verification/area-bomb/active/` for the swap-triggered live capture. A 2×2 is a new match *shape* (no 3-in-a-row), so a `checkSquares` scan (`matrix.ts`) runs alongside `checkMatches` and every "is there a match?" gate consults both (`applyMove` snap-back, `resolveCascades` loop, `hasLegalMoves`, generator/`shuffle` guarantees) — otherwise a square-forming swap snaps back and latent squares spawn free bombs. ~~Still deferred: the L/T-shape trigger~~ — partially resolved: a genuine two-arm crossing-run (a horizontal and a vertical run, each EXACTLY length 3, sharing one cell — the classic L/T/plus) now spawns an area bomb at the shared cell too (`matrix.ts`'s `checkCrossShapes`, additive to `checkSquares`) — see engine/DECISIONS.md's crossing-run entry and `docs/verification/crossing-shape/`. ~~Still deferred, narrower than before: a **square** overlapping a run (not two runs crossing) still stands down exactly as before~~ — **partially resolved**: real playtesting reported a genuine 2×3-rectangle-missing-one-corner shape (a plain exactly-3 run plus a 2-cell extension into the adjacent row/column) not spawning a bomb — investigated and confirmed `checkSquares` (`matrix.ts`) already finds this embedded square unconditionally (no isolation requirement at all); the gap was `resolveMatchEffects` (`engine/gameState.ts`) standing down *every* square touching a run indiscriminately. Fixed with `isUnambiguousEmbeddedSquare`: a square overlapping a run now fires anyway when (1) every run touching its cells is exactly length 3, (2) no *other* detected square shares a cell with it, and (3) it shares no cell with a detected cross. See `engine/DECISIONS.md`'s "Embedded square in a straight run" entry and `docs/verification/embedded-square-in-run/`. **Still genuinely deferred, not silently resolved by this fix**: a *genuinely ambiguous* region with two or more overlapping embedded-square candidates (e.g. a full, aligned 2×3 rectangle where *both* rows are independently exactly-3 runs) — condition (2) above explicitly detects this and stands both candidates down rather than guessing which one wins; which one *should* win is an open design question, not yet decided. An intersection involving a 4- or 5-long arm also still stands down entirely at that cell, leaving the existing striped/color-bomb run logic to fire unchanged. (Blast chaining — an area bomb caught in another effect's clear firing its own 3×3 — is now **built**, see entry (a) above.) ~~A square with a live striped corner used to silently fail to form at all~~ — **resolved**: `checkSquares` now accepts a live striped corner (blocker/void still excluded), and it fires its own sweep instead of a new area bomb spawning — see engine/DECISIONS.md's "Squares: a live striped corner now counts" entry and `docs/verification/square-striped-corner/`.
- ~~area + special combos~~ — **resolved**: all three remaining area-bomb pairings now fire a real combined effect instead of snapping back. **area + color bomb** converts every piece of the board's most-common matchType (`mostCommonMatchType`, the same helper a chained partner-less color bomb already used) into an area bomb and fires them all at once — several 3×3 blasts simultaneously. **area + striped** fires a plus-shaped blast: the area bomb's 3×3 block unioned with the striped piece's full sweep line, built entirely from the existing `areaBlastPositions`/`sweepLinePositions` helpers. **area + area** fires a single bigger 5×5 blast (not two separate 3×3s) — `areaBlastPositions` gained an optional `radius` parameter (default 1, every existing call site unaffected) rather than a second geometry function. All three route through the existing shared `resolveClearSet`, exactly like every other special effect, and `hasLegalMoves` now treats any area-bomb pairing as legal. See engine/DECISIONS.md's "Area-bomb combos: the last three pairings" entry and `docs/verification/area-bomb-combos/`.
- Hidden-piece-underneath blocker mechanic (a blocker conceals a real matchable piece; clearing the blocker layer reveals it, and the revealed piece has to be matched separately) — considered as an alternative to the adjacent-damage mechanic actually built for cling wraps (engine/DECISIONS.md, Phase 6), and rejected for now: it doubles the per-cell data model (a blocker cell would need its own state *and* a full hidden `Piece`) for a payoff — a second, meaningfully different blocker *feel* — that doesn't matter yet with only one blocker type in the skin. Worth revisiting once a second blocker type is real and "every blocker behaves identically" starts to feel thin.
- Recipe box meta layer UI — out of scope for V1 per CLAUDE.md; engine's end-of-level summary event should exist eventually but nothing listens to it yet.
- Cloud asset delivery / per-skin CDN loading — irrelevant until a second skin is real.
- ~~Multi-target objectives~~ — resolved: `GameState`/`LevelConfig`'s `objective` became `objectives`, an array (a single-objective level is length one, no special case); win requires every entry met (`engine/gameState.ts`'s `applyMove`); generated levels place a second objective past a difficulty threshold once the level's own piece-type pool has room for a distinct second target (`appPersistence.ts`'s `generatedObjectiveCount`). See `engine/DECISIONS.md` for the full reasoning. ~~Score-threshold objectives (a numeric score, distinct from counting matched pieces) remain out of scope~~ — resolved: see the scoring-system entry below.
- App Store "distinct product" layout variation — matters only once skin number two ships.
- ~~What actually decrements `GameState.lives` during a level attempt~~ — resolved: losing a level (moves exhausted) now spends exactly one account-level life, applied in `App.tsx` (not mid-level in the engine) and gated at every level-start entry point (`canStartLevel`). The mid-level `pauseReason: 'lives'`/`grantBonusLife` mechanics Phase 4 built for the *other* candidate design (a mid-level lives resource with its own spend trigger) were removed outright rather than kept as unreachable insurance — see `engine/DECISIONS.md`'s Phase 7 section for the full reasoning on why that design was dropped instead of finished.
- ~~Basic lives regen has no visible countdown anywhere~~ — resolved: `OutOfLives.tsx` now ticks a real "Next life in M:SS" countdown (`formatCountdown`), computed fresh once a second from `appPersistence.ts`'s `msUntilNextLifeRegen` against the same `lives`/`livesLastRegenAt`/`regenMinutes` inputs `applyLivesRegen` already trusts — not a separately-tracked value.
- No screen shows the player's current lives count outside of an active level's HUD — Home and All Levels both now gate real gameplay on a live, reactive `lives` value (see `App.tsx`), which makes this a more meaningful gap than it was when this same idea was skipped in the entry below (back when the only available value was a persisted, not-live `SaveData.lives`). Still not built this session since it wasn't asked for, but the original reasoning for skipping it is now partly stale — worth a real look next time Home/All Levels are touched.
- ~~Per-step cascade animation~~ — resolved: `applyMove` now returns `steps: Board[]` (one settled board per cascade pass, last entry equals `state.board`), and `Board.tsx` walks them so each pass animates as its own beat. See engine/DECISIONS.md's cascade-steps entry.
- Literal edge-to-edge fill on all four sides of the board — with square tiles and an 8-row/6-col grid on a typical tall phone screen, tile size is bound by screen *width* (6 columns is the tighter constraint), so there's unavoidable vertical slack even at max tile size. `Board.tsx` now centers the grid in the full available height (see `components/NOTES.md`) rather than dumping all the slack below it, but it isn't literally edge-to-edge top and bottom unless tiles go non-square or the grid's row/col ratio is changed — neither of which was asked for.
- ~~Dashboard is a dead end once every level in `LEVEL_QUEUE` is complete~~ — resolved: past the hand-built queue, `buildLevelConfig` (`App.tsx`) falls through to `buildGeneratedLevelConfig` (`appPersistence.ts`) indefinitely instead of dead-ending, and any hand-built or previously-completed generated level can be replayed via `handlePlayLevel`. Superseded by the Home/All Levels rebuild below — the flat `Dashboard.tsx` this note originally described no longer exists; `resolveStartScreen` no longer "always resumes gameplay directly" either (see that entry).
- `LEVEL_QUEUE` in `App.tsx` is still a hardcoded 3-level array (seed/movesLimit/objective per level), not a real level-authoring pipeline — adding a 4th *curated* level or rebalancing an existing one still means hand-editing that array. No longer blocking on content running out, though: past index 3, levels are generator-driven and unbounded.
- Generated-level difficulty ramp (`generatedPieceTypeCount`/`generatedMovesLimit`/`generatedTargetCount` in `appPersistence.ts`) is a simple linear step-down with hand-picked floors (3 piece types, 18 moves, and a new 10-piece target floor) and a capped target (26) — not tuned against real playtesting, just enough to keep a long session from becoming either trivial or mechanically unwinnable. `generatedMovesLimit`/`generatedTargetCount` now also scale proportionally to a shape template's real playable-cell ratio (see engine/DECISIONS.md's "generated-level difficulty ramp was blind to shape-template playable area" entry, and the board-shape entry below) — a real playtest-reported gap, now closed — but the underlying step sizes/floors/cap themselves are still hand-picked, not playtested. Revisit once there's actual play data past level ~10.
- `components/AllLevels.tsx`'s row list (formerly `Dashboard.tsx`) grows by one for every generated level the player finishes and never prunes — fine at this project's actual scale (one real player, no infinite-scroll concern), but not a paginated/virtualized list if that stops being true.
- Power-up tray UI — the empty `powerUpTray` strip (`Board.tsx` + `skinConfig.powerUpTray` + `config.json`) reserved space beneath the board for icons that were never designed or built, and was pulled entirely rather than left rendering nothing. Removing it lets `boardArea` reclaim that fixed height as (larger) centered slack above/below the board — the same pre-existing vertical-slack tradeoff already logged above, not a new issue. Real power-up UI is out of scope until actual power-up pieces/behaviors are designed (see the row-clearer/blocker entry above); rebuilding the tray at that point should size itself off real content instead of a placeholder height. ~~A free, non-purchasable "fresh board" affordance~~ — **resolved**: a free, uncapped "🔀 Shuffle" button (`Board.tsx`'s `handleRequestShuffle`, `engine/gameState.ts`'s `requestManualShuffle`) reuses the existing stuck-board `shuffle()` rescue for a genuinely free reshuffle with no move/life cost — see `engine/DECISIONS.md`'s shuffle-button entry and `docs/verification/manual-shuffle/`.
- The recipe book's "total" (Home's `{completed} of {total}` and All Levels' enumerable row list) is defined as `resolveVisibleLevelIndices` — the hand-built queue's 3 levels always, plus any generated level actually completed (`components/levelProgress.ts`). This was a deliberate call, not an oversight: the generator is unbounded (see `resolveNextLevelIndex`'s own note), so there's no real fixed content ceiling to lock against the way the design mockup's illustrative "24" implied — inventing a round number would have been exactly the kind of fabricated value this session was told not to hardcode. The real consequence: once a player is past the hand-built queue and current on generated levels, the progress dot row and "X of Y" always read as fully caught up (a completed generated level is only ever added to the list already-completed) — "locked" is only ever observable within the original 3 hand-built levels. Revisit if a real fixed level-count content plan ever exists.
- All Levels renders only two row states (completed/checkmark/tappable vs. locked/dimmed/not-tappable), per this session's written spec. The Claude Design interactive prototype (`skins/lalas-kitchen/design-reference/interactive-prototype.html`) actually shows a third "up next" state — the very next unplayed level gets a highlighted border and an "up next" pill instead of being locked like everything else not yet completed. Deliberately not built this session since the written requirements enumerated exactly two states; the immediate next level is still fully playable, just via Home's "Start cooking" rather than by tapping its row on All Levels. Worth a quick confirm if the three-state look is actually wanted.
~~Home's hero banner fade is a dependency-free approximation of the design mockup's actual CSS gradient fade under the hero image~~ — **resolved**: `expo-linear-gradient` is now installed, and `Home.tsx`'s hero fade is a real `LinearGradient` spanning the full hero height with stops matching the mockup's actual CSS (`rgba(bg,0)` to 55%, `rgba(bg,0.85)` at 85%, solid at 100%), replacing the old 5-band opacity-stack approximation entirely. See `engine/DECISIONS.md`'s font-and-gradient-polish entry and `docs/verification/font-and-gradient-polish/`. (`components/GinghamTrim.tsx`'s checkerboard trim remains a separate, still-gradient-free approximation — a genuine 2-row offset checkerboard, not touched this session.)
~~The design mockups use two custom Google Fonts ('Baloo 2' for headings, 'Nunito Sans' for body) — neither is loaded in this app~~ — **resolved**: `expo-font`, `@expo-google-fonts/baloo-2`, and `@expo-google-fonts/nunito-sans` are now installed (`components/fonts.ts`'s `useAppFonts`/`Fonts`), and every heading/body style in `Home.tsx`/`LevelMap.tsx` (the current Level Map, `AllLevels.tsx`'s successor) sets a real `fontFamily` instead of relying on the system default. Existing `fontSize`/`fontWeight` numbers were left as-is (already documented above as matching the mockup's choices) — only the font family was standing in for the real thing. See `engine/DECISIONS.md`'s font-and-gradient-polish entry and `docs/verification/font-and-gradient-polish/`.
- The mockup's Home hero shows an optional lives/flame-count indicator overlaid on the top-right corner of the header image. Skipped — it isn't in this session's written Home screen requirements (only in the interactive prototype's decorative flourish). Originally also justified by "Home has no in-progress `GameState` to read a live lives count from" — that reasoning is now stale: `App.tsx` has carried a real, reactive account-level `lives` value since the life-spend session (see the entry above), so passing it into `Home` would just be a prop, not a new data source. Still not built since it wasn't asked for, but worth knowing the old blocker is gone if this is revisited.
- Dynamic denial-zone spread — the mechanic itself is built (a gated blocker zone that grows into an adjacent ordinary cell if left unaddressed, with a calm crack/dimming warning the move before — see engine/DECISIONS.md's Phase 8 and `docs/verification/denial-zone-spread/`). Deliberately bounded, deferrals logged rather than built: (a) **spread eats only ordinary cells** — it never converts a player-earned special (striped/color bomb/area bomb) into a blocker, and never double-places onto an existing blocker; letting a zone consume or interact with specials is a follow-up; (b) **no spread chaining/merging beyond becoming one more ordinary blocker** — a spread blocker takes normal adjacent damage like any other; zones don't accelerate, merge, or trigger cascades as they grow; ~~(c) clustered generation is not built~~ — **resolved**: `engine/generator.ts`'s `clusteredPositions` (gated behind a new `GeneratorConfig.clusterBlockers` flag, translated from `LevelConfig.denialSpread` in `engine/gameState.ts`'s `createGameState`) grows one contiguous region via random adjacent-cell growth instead of scattering blockers independently, applied only to denial-zone-eligible levels (`generatedLevelNumber >= DENIAL_SPREAD_MIN_LEVEL_NUMBER`) — every level below the threshold, and every hand-built level, keeps the original scatter placement untouched. See `engine/DECISIONS.md`'s clustered-denial-zone-placement entry and `docs/verification/clustered-denial-zone-placement/`. `SPREAD_MOVE_FRACTION` (0.25) and `DENIAL_SPREAD_MIN_LEVEL_NUMBER` (10) are still hand-picked, not playtested — revisit alongside the generated-level difficulty ramp once there's real play data past level ~10.
- Board shape / void cells — non-rectangular boards are now **built** (a `type: 'void'` sentinel piece makes cells non-playable; segmented per-column gravity treats a void as a fixed floor; matching/legal-moves/shuffle/generator/render are all void-aware; `LevelConfig.voidCells` carves the shape; the plus-shaped Level 4 "Cutting Board" showcases it — see engine/DECISIONS.md's board-shape entry and `docs/verification/board-shape/`). Deliberately bounded, deferrals logged rather than built: ~~(a) generator-driven shapes~~ — **resolved**: `engine/boardShapes.ts` now has a small curated set of 3 reusable templates (`cut_corners`, `plus`, `ring`, each a pure `(rows, cols) => Position[]`), and `appPersistence.ts`'s `generatedShapeId(levelNumber)` gates their introduction the same way `BLOCKER_MIN_LEVEL_NUMBER`/`DENIAL_SPREAD_MIN_LEVEL_NUMBER` gate their own features — no shape below generated level number 8, then a 1-in-4 cadence rotating through the 3 templates, wired into `buildGeneratedLevelConfig`'s `voidCells` output. `generateLevel` itself needed zero changes — the void-cell contract was already fully general (blockers structurally excluded from void positions, shuffle holds voids fixed, match/legal-move gates already void-aware). See engine/DECISIONS.md's "Generator-driven board shapes" entry and `docs/verification/generator-driven-board-shapes/`. ~~(b) voids × specials interaction~~ — **resolved**: a real playtest found that a sweep/blast/detonation reaching a void did **not**, in fact, simply skip it — five clear-set builders (the in-match striped sweep, chain expansion, the area-bomb blast, the color-bomb whole-board detonation, and the striped-combo clear-set helper) were written before voids existed and only ever excluded `'blocker'`, so on a shaped board they could clear straight through a void, which segmented gravity then permanently refilled as an ordinary gap. Fixed with one shared `isClearable` predicate at all five sites — see engine/DECISIONS.md's "Fixed: the ... claim above didn't hold" entry and `docs/verification/void-specials-clear-fix/`. Void × **denial-spread** interaction remains correctly excluded and un-exposed to this bug (the spread already only ever targets `'normal'` cells, never a void) — there's no special that *creates*, *removes*, or *moves* a void, and no shape that changes mid-level. (c) **the "board always full" invariant assumes reachable pockets** — segmented gravity refills each enclosed playable pocket from its own local top (a piece appears mid-board), which is correct and calm but means a shape author can't rely on refills *flowing* between pockets separated by a void; a diagonal/flow gravity model was explicitly rejected as overkill (see the DECISIONS entry). ~~(d) the difficulty ramp never accounted for a shape template's reduced playable area~~ — **resolved**: a real playtest report on a `ring`-shaped level (55% playable at the real 8x5 generated-board size — the most restrictive of the 3 templates) confirmed `generatedMovesLimit`/`generatedTargetCount` computed their numbers from `levelNumber` alone, with no awareness that a shape had just removed cells; both now scale proportionally to `engine/boardShapes.ts`'s new `playableCellRatio(rows, cols, voidCells)`, floored the same way their unscaled versions already were — see engine/DECISIONS.md's "generated-level difficulty ramp was blind to shape-template playable area" entry.
- Ad/monetization: the platform-selected `AdService` abstraction (`services/adService.ts` — `requestRewardedAd()`/`requestBannerAd()`/`isRewardedAdAvailable()`, selected between an AdMob adapter and a CrazyGames adapter by `Platform.OS`) is now **built**, but real SDK integration is not — both adapters are stubs that instantly resolve the reward, matching the two "watch a video" flows' pre-abstraction behavior (see engine/DECISIONS.md's ad-service entry). CrazyGames' "Basic Launch" gap — the platform disables all monetization until it reviews and graduates a game to "Full Launch," with no SDK call to detect this proactively — is also now **built**: `crazyGamesAdService.ts`'s `CRAZY_GAMES_MONETIZATION_ENABLED` build-time flag (manually flipped, since no runtime check exists) gates both the grant (free when disabled, matching mobile's real-ad path when enabled) and the button copy on `ContinueOffer`/`OutOfLives` (the grant CTA moved from `PausedOverlay` to the new `ContinueOffer` in the mid-level-continue session — see `engine/DECISIONS.md`'s mid-level-continue entry) via the new `isRewardedAdAvailable()` — see engine/DECISIONS.md's CrazyGames Basic Launch entry and `docs/verification/crazygames-basic-launch/`. ~~Deferred: (a) wiring in either real SDK (AdMob, CrazyGames)~~ — **both halves are now resolved**: `services/expoGoogleMobileAdsService.ts` wires in the real `react-native-google-mobile-ads` SDK (a real load→show→listen cycle for `requestRewardedAd`), using Google's own publicly documented demo App/ad-unit ids (confirmed via direct investigation, not memory) — see `engine/DECISIONS.md`'s real-admob-sdk entry. **Not verified live**: this is a native module requiring a real device/dev-client build, which doesn't exist in this environment — the same standing gap every other native-only feature here carries. `services/crazyGamesAdService.ts`'s Full Launch path now wires in the real CrazyGames SDK v3 (a browser `<script>` tag, not a native module — genuinely testable with an injected fake, and confirmed to have a real local-testing mode on localhost requiring no registered game listing) — see `engine/DECISIONS.md`'s real-crazygames-sdk entry. **Verified up to, but not including, the final in-app script execution**: the button/gate logic was confirmed live; the actual script injection-and-run was blocked by this environment's own safety guardrail (unauthorized external code execution) and that block was respected, not worked around — a narrower, different gap than AdMob's "no native device" one. ~~(b) `adService.ts` eagerly statically imported both stub adapters~~ — **resolved, and a real instance of the risk this note predicted was found and fixed**: `selectAdService` now takes its mobile/web services as injected params (mirroring `hapticsService.ts`'s own `selectHapticsService`). The Metro-resolved `adService.native.ts`/`adService.web.ts` split this note floated as a hypothetical fallback turned out to be genuinely necessary, not avoidable: wiring in the real AdMob SDK broke the entire web bundle (`react-native-google-mobile-ads` transitively imports a native-only codegen component Metro can't bundle for web), caught live while testing the CrazyGames work, fixed with `services/defaultAdService.native.ts`/`defaultAdService.web.ts`. (c) **`requestBannerAd()` has no current caller** — still true: built into the interface, but no banner-ad UI exists in the game yet, so both real adapters keep it an honest no-op stub rather than loading a real ad nothing displays. (d) **no automatic detection of Full Launch graduation** — CrazyGames genuinely has no SDK-level signal for this today (confirmed by direct doc investigation, not assumed), so `CRAZY_GAMES_MONETIZATION_ENABLED` will need a real developer to flip it by hand whenever CrazyGames actually notifies of graduation; worth revisiting only if CrazyGames ships a proactive check later. (e) **CrazyGames' own recommendation to mute/pause gameplay while an ad plays isn't built** — every current caller only ever requests an ad from an already-paused overlay, so the practical gap is narrow, but it's a real, disclosed scope line. (f) **`react-native-google-mobile-ads` is pinned to `16.0.0`, not the latest published `16.4.0`** (see `engine/DECISIONS.md`'s "First real Android build attempt fails on a Kotlin metadata mismatch" entry) — a real Android build proved every `play-services-ads` release new enough for `16.1.0`+'s own wrapper code (`setAgeRestrictedTreatment`, added in play-services-ads 25.3.0) is already compiled with Kotlin metadata newer than react-native 0.81.5's own bundled `@react-native/gradle-plugin` can read, and that ceiling can't be worked around from this project's own config (it's hardcoded in a vendored `node_modules` file, and the Kotlin Gradle Plugin only supports one effective compiler version per build). This means age-restricted-treatment consent support (and anything else in 16.1.0–16.4.0) is unavailable until either react-native's own bundled tooling bumps its Kotlin past 2.3.0, or a future react-native-google-mobile-ads release adds a way to opt out of whatever forced its own play-services-ads floor up to 25.3.0+. Revisit by re-attempting the same real-build verification whenever react-native or react-native-google-mobile-ads next gets upgraded.
- Web export subpath paths: `scripts/fix-web-export-paths.js` (run via `npm run export:web`) is now **built and verified live** — a real `expo export -p web`'s root-absolute paths (`/_expo/...`, `/assets/...`) 404 when the export is mounted under a subpath (e.g. CrazyGames' per-game folder), and the script rewrites them to relative paths post-export; confirmed against a real export served from a real subpath one level below server root, driven by a real browser over CDP, with zero failed requests for anything the app loads — see engine/DECISIONS.md's web-export-subpath-paths entry and `docs/verification/web-export-subpath-paths/`. Deliberately narrow, deferred rather than built: (a) **it's a manual post-export step**, not wired into any CI/deploy pipeline — nothing automatically runs `npm run export:web` (vs. plain `expo export`) before a real CrazyGames upload, so a developer has to remember to use the right script; worth automating once a real deploy pipeline exists (there isn't one yet — this is still a local, unpublished project); (b) **only handles the one root-absolute-path failure mode** actually found in a real export (script src, sprite asset URLs) — if a future Expo/Metro version emits some other subpath-unsafe construct (e.g. an absolute `import()` chunk URL), the regex won't catch a shape it doesn't already match; the context-based match (quote + leading slash + word char) was chosen specifically so it keeps working across *asset-layout* changes, but a genuinely new URL *shape* would need a fresh look, not an assumption that this script covers it forever.

- Distinct animation identities for the color bomb detonation, the cross combo, and the supercombo are **built** this session — see engine/DECISIONS.md's special-effect-identity entry and `docs/verification/special-effect-identity/`. Deliberately narrow, deferred rather than built: (a) **a color bomb reached via chaining** (caught in another effect's clear, per the special-piece-chaining entry above, rather than the bomb the player actually swapped) still gets the flat generic clear — the new radial ripple only applies to the swap's own origin, since a chained bomb has no swap position to radiate from (it detonates the board's most-common color from wherever it happened to be sitting); giving that case its own identity (e.g. radiating from the chained bomb's own resting position) is a real, scoped follow-up, not attempted this session; (b) **the per-link chaining flash remains separately deferred** — this session's two-beat supercombo treatment only covers the supercombo's own converted pieces, not the general "a special caught in ANY effect's clear fires its own effect too" chaining moment (see the still-deferred per-link animation flash in the entry above) — the two are visually similar problems but distinct scopes, and only the supercombo one was in this session's ask; (c) **no distinct identity for a caught-via-chaining area bomb or striped piece inside a color-bomb/cross/supercombo pass** — those already correctly fire their OWN real effect (a chained striped piece's real sweep, a chained area bomb's real powder burst) via the existing per-piece derivations, so they were already fine and untouched, but they don't get any NEW visual tie-in to the outer effect that caught them (e.g. no "this was caught by the wave" cue) — cosmetic, not attempted.
- `chain_reaction` tutorial (the fourth one-time card, teaching the moment 2+ specials fire together — see engine/DECISIONS.md's `chain_reaction` entry) is **built**, but three things around it are deliberately narrow: (a) **one generic card for both triggers** — the copy doesn't distinguish "this was a chain reaction" from "this was a combo," even though `multiSpecialFired` can't tell them apart today either (it's a pure count, not a cause); splitting the copy by cause would need the count threaded alongside *which* mechanism produced it, not just that 2+ specials fired; (b) **no dedicated icon art, and no zero-code path to add it later** — unlike the three per-piece tutorials (whose icon is `getSpriteForPiece(piece, config)`, so real art is one `spriteRegistry.ts` line), chain_reaction's `piece` prop is always `null` and never consults the registry at all; giving it real art later needs a small code change to `SpecialTutorialOverlay.tsx`, not just a registry entry; (c) **same-move collision with a freshly-spawned per-piece tutorial always resolves in chain_reaction's favor** — if a single move both mints a not-yet-seen special AND is a genuine multi-special moment, chain_reaction wins (it's set first, synchronously, in `Board.tsx`'s `animateCascade`) and the per-piece tutorial simply waits for a later qualifying move; this was a judgment call (celebrating the moment tied to the animation that just played), not something confirmed against real playtesting since the scenario is rare and hasn't been observed live yet.

- Calm stuck-player hint (`engine/matrix.ts`'s `findAnyLegalMove`, `components/Board.tsx`'s hint button/`hintPair`/`hintUsesUsed`, `components/Tile.tsx`'s `HintGlowOverlay`, `components/pauseActions.ts`'s `canUseHint`/`HINT_USES_PER_ATTEMPT` — see engine/DECISIONS.md's stuck-player-hint entry) is **built**: a real, always-visible "💡 Hint" button (no more automatic idle timer — that approach was tried at 8s, then retuned to 18s, then abandoned entirely once feedback showed no fixed threshold avoids sometimes interrupting genuine thinking) glows a real legal move on tap, capped at 2 uses per level attempt via the same per-attempt cap mechanism the bonus-moves grant uses. Deliberately not built: (a) **no persisted "seen enough, stop showing" throttle** — unlike sound's off-default, there's no real user-research signal yet that this specific player would want the button hidden after using it a lot; if real play surfaces that, a `SaveData` flag is the natural follow-up, not guessed at now; (b) **the 2-use-per-attempt cap is a hand-picked number, matched to the bonus-moves grant's own cap rather than independently tuned or made configurable** per-skin/per-difficulty — the two caps happen to share a value today but are tracked by fully independent counters, so either could change without touching the other; (c) **no distinction in the hint's visual treatment by which special (if any) the legal move would involve** — a hinted swap that would fire a color bomb, an ordinary 3-match, and a square-forming swap all glow identically; teaching "this specific move does something extra" was never asked for and would risk reading as a tip rather than a calm nudge.

- Level map (`components/LevelMap.tsx`, replacing the old scrollable `AllLevels.tsx`, plus persisted best-ever star ratings in `SaveData.levelStars` — see engine/DECISIONS.md's level-map entry) is **built**. Deliberately not built this session: (a) **no `react-native-svg` dependency** — the winding path is straight rotated-View segments between node centers (`components/levelMapLayout.ts`), not a smooth bezier curve like the approved HTML mockup; adding a real SVG (or gradient) dependency for a closer curve is a real, reversible follow-up if the straight-segment look reads as insufficiently "winding" in practice; (b) **no ambient garden decor** (the mockup's trellis arch, potted herbs, kitchen-window/steam-wisp motif) — illustrative flourishes in the approved mockup, not part of "path and node states match the approved design," and translating hand-drawn SVG art into RN-View approximations is real bespoke illustration work nobody asked for this session; (c) **no ingredient icon or level display name on a node** — the approved mockup shows only the level number plus its status decoration; adding either back is a small, obvious follow-up if the map ever feels too anonymous; (d) **no theme/dark-mode system** — confirmed none exists anywhere in this app; the map is single-theme (matching every other screen) by explicit architect decision, with the dark variant from the design-approval session kept only as a future reference, not built as a byproduct here; (e) **`MAP_LOCKED_LOOKAHEAD` (4) is a fixed, hand-picked preview depth**, not tuned against real playtesting or made configurable.

- `board_shape`/`spread_warning` tutorials (the sixth and seventh one-time cards — see engine/DECISIONS.md's "Two more one-time tutorials" entry) are **built** and fully verified live. One narrow, structural gap logged rather than glossed over: `board_shape` has **no dedicated icon art, and — unlike the three per-piece tutorials — never can**. A striped/color-bomb/area-bomb card's icon is a registry addition away from real art (`getSpriteForPiece` resolves it from the actual forged piece); `board_shape` has no single piece to anchor an icon to at all (a void cell renders no Tile), the same structural bind `chain_reaction`/`how_to_play` are already in (see entry above on `chain_reaction`'s icon). Giving it bespoke art later means a small `SpecialTutorialOverlay.tsx` change (e.g. a fixed "gap" glyph), not a registry entry. `spread_warning` has no such gap — its icon is the real warned piece's own sprite, the same "show the real thing" convention the blocker tutorial uses. Nothing else about either tutorial is deferred; both are complete against this session's brief.

- Scoring system / `score`-type objectives (`ObjectiveType`'s `'score'` variant, `SCORE_TIER_POINTS`/`passScoreMultiplier` in `engine/gameState.ts` — see engine/DECISIONS.md's scoring-system entry) are **built**: every cleared cell scores at one of three tiers (ordinary/special/bomb) depending on the mechanism that cleared it, cascades/chains score at an increasing per-pass multiplier, and a `'score'` objective tracks cumulative move-score instead of one matchType's count. One hand-built level (App.tsx's `LEVEL_QUEUE` — "Score Rush", level 5) uses it; verified live over CDP — see `docs/verification/score-objective/`. Deliberately narrow, deferred rather than built: (a) **the point values and cascade-bonus rate are hand-picked, not playtested** — `SCORE_TIER_POINTS` (10/25/50) and `CASCADE_CHAIN_BONUS_PER_PASS` (0.25) are a first real judgment call, wide enough to feel different on the HUD but not validated against actual play; revisit once there's real data on how fast a score target should feel reachable; ~~(b) `buildGeneratedLevelConfig` (`appPersistence.ts`) never produces a `'score'` objective~~ — **resolved**: `isScoreObjectiveLevel`/`generatedScoreTarget` (`appPersistence.ts`) now place one past a gate+cadence (levelNumber >= 3, every 3rd), calibrated against Score Rush's own 1000/24 density, only ever replacing a level's single objective (never mixed with a second `'collect'` target) — see `engine/DECISIONS.md`'s generator-driven-score-objectives entry and `docs/verification/generator-score-objectives/`. The gate/cadence numbers and point density remain hand-picked, not playtested. (c) **blockers cleared by adjacent damage always score at the ordinary tier**, regardless of what triggered the clear (a striped sweep's own adjacent blocker damage scores the same as a plain match's) — a real simplification, not an oversight, since a blocker never forms a run/special itself; (d) **no distinct scoring identity for chained vs. combo vs. solo detonations beyond their shared tier** — a striped+striped cross, a striped+bomb supercombo, and a solo color bomb all score at the same flat 'bomb' tier per cell; giving combos their own even-higher tier was considered and explicitly not done (see the DECISIONS.md entry) to keep the tier count at three, not four-plus.

- Clearance layers / `'clearance'`-type objectives (`ObjectiveType`'s `'clearance'` variant, `GameState.layerCells`, `decrementLayers` in `engine/gameState.ts` — see engine/DECISIONS.md's clearance-layers entry) are **built**: a hidden per-cell layer count (1 or 2, the level author's choice) decrements whenever the piece sitting on that cell is cleared by any effect, winning once every layered cell reaches 0. One hand-built level (App.tsx's `LEVEL_QUEUE` — "Dusty Counter", level 6) uses it; verified live over CDP — see `docs/verification/clearance-layers/`. Deliberately narrow, deferred rather than built: ~~(a) `buildGeneratedLevelConfig` never produces `layerCells` or a `'clearance'` objective~~ — **resolved**: `isClearanceObjectiveLevel`/`generatedLayerCells` (`appPersistence.ts`) now place one past a gate+cadence (levelNumber >= 5, every 4th), density-matched to Dusty Counter's own ratio and scaled to the real playable-cell count on a shaped board — see `engine/DECISIONS.md`'s generator-driven-clearance-objectives entry and `docs/verification/generator-clearance-objectives/`. This resolved (b)'s "blocker+layer coexistence" question in the SIMPLEST possible direction, not the richest one: a clearance-gated generated level gets NO blockers at all (`blockerCount` forced to 0), sidestepping the harder problem of placing layers around wherever `generateLevel`'s own seeded RNG happens to put blockers (genuinely unknown ahead of time, unlike `voidCells`, which are pure functions of rows/cols known in advance). A real per-cell-aware placement (layers avoiding specific blocker positions on a level that still has blockers elsewhere) remains open if ever wanted. (b) **a layered cell never coexists with a blocker or a void on the same cell** — a real, confirmed judgment call (not silently assumed): a layered cell is exclusively an ordinary/special matchable piece this session. Blocker+layer coexistence (does a blocker's own final adjacent-damage clear also decrement the layer underneath it, or only a direct force-clear?) is a genuinely separate design question, deliberately not decided here; (c) **the layer count range (1-2) and `LAYER_OPACITY_STEP`'s wash opacity are hand-picked, not playtested** — matches the existing blocker-hits convention for a comfortable default, but revisit once there's real play data. (d) **the generator's own score/clearance cadence-coincidence tiebreak (score always wins when both gates land on the same levelNumber) is a hand-picked resolution, not a deliberately balanced design** — the two cadences were never staggered to avoid overlapping in the first place, a real, acknowledged tradeoff rather than an oversight.
- ~~Background-music loop is not wired into `soundRegistry.ts`/any playback service~~ — was resolved, **then removed again**: the loop was wired in (`musicRegistry`, `expoAudioSoundService.ts`'s `playMusic`/`stopMusic`, `components/backgroundMusic.ts`'s `syncBackgroundMusic`), gated by the same `soundEnabled` toggle as the one-shot cues and scoped to gameplay only — but was later taken out of active gameplay entirely (all of that wiring deleted outright, the same discipline the automatic hint timer got), keeping match/cascade/win completely untouched. `background.wav` itself was deliberately left on disk, unreferenced, as real reusable work should the decision reverse again. See `engine/DECISIONS.md`'s background-music-loop and background-music-loop-removal entries, and `docs/verification/background-music-loop-removal/`. The two previously-disclosed gaps (no human has listened to the loop; no native device test) are now moot — there's no loop left to listen to or test.

- The tutorial-cadence throttle (`appPersistence.ts`'s `TUTORIAL_MIN_GAP_MS`/`canShowTutorialNow`/`shouldActivateTutorial`, `components/Board.tsx`'s `activeTutorialId`/`nextEligibleTutorialId` — see engine/DECISIONS.md's tutorial-cadence-throttle entry) is **built**: a real 60-second minimum gap between any two of the seven one-time tutorials actually appearing, so two genuine firsts (e.g. a shaped, blockered generated level's `board_shape`+`blocker`, or forming a striped piece then a color bomb on consecutive moves) defer instead of stacking, rechecked on every real move commit rather than a background timer. Verified live, including a real gap the capture itself found and fixed (a deferred mount-time tutorial's cooldown clearing on the exact move that wins/loses the level used to risk popping over the Won/Paused overlay — now gated on `gameState.status === 'in_progress'`) — see `docs/verification/tutorial-cadence-throttle/`. Deliberately narrow, deferred rather than built: (a) **a still-pending mount-time tutorial (`board_shape`/`blocker`) whose level ends (win or loss) before its cooldown clears is never shown for that specific level** — it isn't lost forever, since `shouldShowBoardShapeTutorial`/`shouldShowBlockerTutorial` re-derive fresh from scratch at the next level's own mount and will show then if that level also triggers the condition, but the player doesn't get the explanation anchored to the exact board that first surfaced it; a real fix (e.g. carrying a genuine "still owed" queue across level boundaries) wasn't asked for and adds real state-lifetime complexity for a narrow edge case. (b) **`TUTORIAL_MIN_GAP_MS` (60 seconds) is a hand-picked judgment call, not playtested** — reasoned from the existing 18s single-thinking-pause idle-hint research (a tutorial-to-tutorial gap should read as noticeably longer than one pause), but no real player data confirms 60s specifically feels right rather than, say, 45 or 90.
- First accessibility pass (font-scale cap + `AppText.tsx` wrapper across all 16 `Text`-importing components, `allowFontScaling={false}` on icon-glyph/fixed-diameter elements, a real CVD-simulation check of the six ingredient + three special sprites — see `engine/DECISIONS.md`'s "First accessibility pass" entry) is **built**. Deliberately narrow, deferred rather than built: (a) **no real Android/iOS device or emulator confirms the actual OS-level text-size setting** — this session's `?fontScaleDebug=N` in-tree simulation faithfully exercises the real component styles (react-native-web's `PixelRatio.getFontScale()` always returns 1, so there's no browser-native way to test this), but a genuine device/simulator test is the same standing disclosed gap every other native-only feature in this project carries; (b) **`WonOverlay`/`PausedOverlay`/`ContinueOffer`/`OutOfLives` were confirmed clip-safe by file-by-file code audit (no fixed-height `Text` containers exist in any of them) rather than independently screenshotted at 2x** — a real screenshot only proves one specific copy length anyway, so the structural audit is arguably the stronger claim, but it wasn't paired with a live capture the way Home/board-HUD/LevelMap were; (c) **only `accessibilityLabel` was added or confirmed — no `accessibilityRole`/`accessibilityHint` audit was done**, since the brief scoped this pass to font-scale and colorblind-safety specifically, not a full screen-reader pass; a genuine VoiceOver/TalkBack pass (roles, hints, focus order, live regions for HUD count changes) is real, separate follow-up work; (d) **no additional non-color visual cue was added to any piece** — the CVD simulation found the existing sprite shapes already sufficient, so nothing was added; revisit only if real colorblind playtesting ever disagrees with the simulation.

- App icon (`assets/icon.png` wired into `app.json`'s `icon`/`ios.icon`/`android.adaptiveIcon.foregroundImage`+`backgroundColor`/`web.favicon`, plus the checked-in `android/app/src/main/res/mipmap-*` files replaced directly — see `engine/DECISIONS.md`'s app-icon-recovery entry) is **built**, recovered from an orphaned local EAS build (`7d846df0...`, unreachable both locally and on GitHub) rather than recreated: the IPA/APK for that build were downloaded before their listed artifact expiration and real shipped icon images extracted directly (Apple's loose CgBI-optimized bundle-root PNGs, hand-decoded; the Android APK's real per-density `ic_launcher*.webp` originals, no re-derivation), and the true 1024×1024 master (unrecoverable from `Assets.car`'s compiled/compressed format with tools available in this environment) was separately supplied by the user from the same original source and verified by direct IHDR inspection plus a visual match against the extracted art before being wired in. Deliberately narrow, disclosed rather than smoothed over: (a) **`android.adaptiveIcon.foregroundImage` reuses the same flattened 1024 master** — the original build's config referenced a distinct `adaptive-icon-foreground.png` (presumably a safe-zone-padded, transparent-background layer), but no pixels for that specific file were ever recovered from either build artifact; an OEM launcher's circular/squircle adaptive mask will crop into the flattened image's outer margin exactly as it would for any icon with no inset, a real but minor cosmetic gap; (b) **this only matters for a future iOS rebuild** — Android builds in this project have never run `expo prebuild` (the `android/` folder is hand-maintained and committed as-is), so `app.json`'s `adaptiveIcon` field is inert for today's Android builds; the actual fix there was the direct native mipmap replacement; (c) **no true CAR-format (`Assets.car`) parser exists in this toolchain** — a raw byte scan for embedded PNG signatures found zero, confirming CoreUI's compiled catalog uses Apple's own structured/LZFSE-style internal format; recovering a *higher*-resolution iOS asset directly from a future compiled catalog (rather than from loose bundle-root files or a separately supplied master) would need a real macOS-side tool (`assetutil`/`actool`) this environment doesn't have.

- Defensive save loading, an app-level `ErrorBoundary`, and a hardened `shuffle` rescue fallback (a real playability-audit's two flagged-critical findings plus one closely related gap found investigating them) are **built** — see engine/DECISIONS.md's "Three real-playability-audit safety fixes" entry and `docs/verification/safety-hardening/`. Deliberately narrow, deferred rather than built: ~~(a) no crash-reporting/telemetry service~~ — **partially resolved**: `ErrorBoundary.tsx`'s `componentDidCatch` still logs via `console.error`, but now also persists the crash into the real save (`SaveData.lastCrash`, `engine/gameState.ts`'s `recordCrash`), calmly surfaced on `components/Settings.tsx` — a real, checkable signal with no new dependency or external account, architect-chosen over a remote service (Sentry or similar) given this project's actual scale. Still genuinely open: no PROACTIVE/remote signal exists — someone has to physically open Settings on the device to ever see it. See `engine/DECISIONS.md`'s in-app-crash-log entry and `docs/verification/crash-telemetry/`. (b) **no native (iOS/Android) device test** for any of the three fixes — same standing disclosed gap as every other feature in this project, no attached device/simulator exists in this environment. (c) **no live click-through of actual gameplay exercising the modified `applyMove`/`shuffle` code path** — a CDP synthetic-mouse-event attempt against React Native Web's gesture-handler-backed `Pressable` didn't register a real swap; confidence instead rests on the engine test suite (521 tests, including a real adversarial ring-board scenario), which is the right tool for engine-logic correctness — worth a fresh live attempt (e.g. real touch-event dispatch instead of mouse events) if this specific gap ever needs closing. (d) **`forceLegalMove`'s two templates (in-line 4-window, three-plus-off-axis-notch) cover every board shape this project currently ships** (rectangles, `cut_corners`, `plus`, `ring` — confirmed live against the real, most-restrictive `ring` case) but aren't a mathematical proof of coverage for every conceivable future shape template; a hypothetical shape with neither a long straight run nor any perpendicular neighbor anywhere could still hit `shuffle`'s final throw — revisit if a 4th board-shape template is ever added to `engine/boardShapes.ts`.
