# Deferred Complexity

One line per deferred idea, with a short reason it was deferred. Parked is fine. Forgotten is not.

- Tuning-constant review â€” done: a full inventory of every hand-picked constant across the generator/difficulty ramp, cross-referenced for genuine inconsistencies. See `engine/DECISIONS.md`'s tuning-constant-review entry. Found and fixed two real gaps (a freshly-spread blocker losing its `specialOnly` flag when a sealed_jar-gated level's denial zone also spreads; clearance objectives never receiving breather relief on their actual workload, unlike collect/score), corrected two stale comments, and confirmed several existing claims by direct computation rather than trusting the prose. Everything else inventoried was already honestly disclosed as hand-picked-not-playtested at the point each feature was built â€” nothing further needed re-disclosing.
- Board topology (conveyors, portals, wraparound edges) â€” investigated, not built, no reasonable bounded scope found. See `engine/DECISIONS.md`'s board-topology entry: every real idea in this space requires a piece to cross a column boundary during gravity resolution, but `engine/matrix.ts`'s `calculateCascades` is structurally a per-column-only algorithm with zero cross-column data flow â€” this isn't a missing branch, it's a different algorithm shape. It's also the single most heavily-relied-upon piece of the engine (void-cell shapes, denial-zone spread, every special piece's refill, the dropdown mechanic, and blocker depth all sit on top of its per-column guarantee), so a structural rewrite risks regressing all of them at once in ways the current test suite â€” which asserts today's contract, not a hypothetical cross-column one â€” wouldn't necessarily catch. Revisit only as its own dedicated phase (redesign `calculateCascades` itself, verify in isolation, then layer a topology feature on top), not as an incremental addition.
- Blocker depth (`specialOnly` blocker, skin id `sealed_jar`) â€” built: a blocker variant that only takes adjacent damage from a special effect (sweep/blast/detonation/chain), never a plain ordinary match, gated to generated levels at or past level 12. See `engine/DECISIONS.md`'s blocker-depth entry and `docs/verification/blocker-depth/`. Deliberately narrow, deferred rather than built: (a) **no distinct visual overlay** beyond the blocker's own sprite â€” matches the precedent every other blocker variant (cling/dish_stack/pot_lid) and the dropdown piece already set (sprite-only, no dedicated overlay component); ~~(b) **no dedicated tutorial** teaching "this blocker needs a special piece"~~ â€” **resolved** (1.0.1 release pass): the 8th one-time card (`SPECIAL_ONLY_BLOCKER_TUTORIAL_ID = 'special_only_blocker'`, mechanic-named per the leak test, detection keyed on the engine `specialOnly` flag never the skin id) now fires at mount the first time a board contains one, showing the real jar sprite, slotted after the generic blocker card in priority and riding the existing cadence throttle â€” see `engine/DECISIONS.md`'s 1.0.1 release-pass entry; (c) **the gate level (12) and hit count (1) are hand-picked, not playtested** â€” whether sealed_jar should be tougher (2 hits) or gated earlier/later is a real, undecided tuning question. ~~(d) **no dedicated sprite art â€” the blocker rendered as the "SE" text-label placeholder**, disclosed only inline in `engine/DECISIONS.md`'s own blocker-depth entry and in `docs/verification/blocker-depth/`'s screenshots (item (a) above's "beyond the blocker's own sprite" phrasing was itself ambiguous about whether that sprite actually existed), but never actually itemized here alongside (a)â€“(c) the way the mechanic's other gaps were~~ â€” **fixed**: a real illustrated jar sprite (`skins/lalas-kitchen/sprites/sealed_jar.webp`) is now registered in `spriteRegistry.ts`, following the same filename-keyed pattern as the other three blockers. Surfaced by a real playtest report of a piece rendering as "SE" text; investigation confirmed it wasn't a regression â€” no sealed_jar sprite had ever been registered since the feature first shipped. Verified live: real generated `sealed_jar` blockers on generated level 20 now render the jar art, with zero "SE" text nodes remaining anywhere on the page â€” see `docs/verification/sealed-jar-sprite-art/`.

- Dropdown ingredients (escort mechanic) â€” built: a colorless `'dropdown'` piece type immune to every clearing effect, collected purely by reaching the bottom of its column via ordinary gravity, feeding a new `'escort'`-type `Objective`. See `engine/DECISIONS.md`'s dropdown-ingredients entry and `docs/verification/dropdown-escort-mechanic/` (which also documents a real crash this feature caught live in `components/Board.tsx`'s animation pipeline, since fixed). Deliberately narrow, deferred rather than built: (a) **`buildGeneratedLevelConfig` never produces `dropdownPositions`/an `'escort'` objective** â€” hand-built content only (one level, "Delivery Day"), matching the same incremental pattern score/clearance objectives both started with before their own later generator-integration sessions; (b) **no dedicated fall/collection animation** â€” a dropdown piece uses the exact same generic cascade-fall visual every other piece already gets, no distinct identity (unlike the striped/color-bomb/area-bomb specials, which each got their own animation-identity session); (c) **the two `dropdownPositions` chosen for "Delivery Day" (both top-row) are a hand-picked, not playtested, starting difficulty** â€” how many dropdown pieces, where they start, and how that should scale with a level's move budget are all real, undecided tuning questions. ~~(d) **no dedicated sprite art â€” the piece rendered as the "DR" text-label placeholder**, disclosed only inline in `engine/DECISIONS.md`'s own dropdown-ingredients entry and in `docs/verification/dropdown-escort-mechanic/`'s screenshots, but never actually itemized here alongside (a)â€“(c) the way the mechanic's other gaps were~~ â€” **fixed**: a real illustrated basket sprite (`skins/lalas-kitchen/sprites/dropdown.webp`) is now registered in `spriteRegistry.ts`, following the exact `area_bomb.webp` one-line pattern. Surfaced by a real playtest report of a piece rendering as "DR" text; investigation confirmed it wasn't a regression â€” no dropdown sprite had ever been registered since the mechanic first shipped. Verified live: a real dropdown tile on "Delivery Day" now renders the basket art, with zero "DR" text nodes remaining anywhere on the page â€” see `docs/verification/dropdown-sprite-art/`.

- ~~`isValidSaveData` (`engine/gameState.ts`) never actually validates `consecutiveLosses`'s type if present~~ â€” **resolved** (1.0.1 release pass): the exact one-line fix this entry spelled out is now in, alongside a loadSave fallback test mirroring the other malformed-optional-field cases.
- Save-data backup/export beyond the OS-level default â€” investigated (see `engine/DECISIONS.md`'s save-backup entry), not built as a new feature. Android already gets Auto Backup to Google Drive for free (`android:allowBackup="true"`, Expo's own prebuild default, unchanged). iOS did NOT â€” `@react-native-async-storage/async-storage` excludes its own storage from iCloud backup by default â€” fixed with a one-line `app.json` config flip (`ios.infoPlist.RCTAsyncStorageExcludeFromBackup: false`), not new infrastructure; unconfirmed on a real device (no iOS device/build exists in this environment, the standing disclosed gap every native-only feature here carries). Web (`localStorage`) has no OS-level backup at all and was deliberately left as an accepted gap: no account/backend system exists to sync against, and one wasn't built for a single-player personal project. A manual export/import (copy a backup code, or a real file via a share sheet) was considered and would close it, but adds real UI/dependency surface for a rare need â€” revisit only if losing web progress ever becomes a real, repeated pain point.

- Sound/haptics stub layer: the infrastructure (services/soundService.ts, services/hapticsService.ts, SaveData flags, Home's toggle, event wiring in Board.tsx) is **built** â€” see engine/DECISIONS.md's sound/haptics stub-layer entry. ~~(a) a real audio backend~~ â€” **resolved**: `services/expoAudioSoundService.ts` (real `expo-audio` playback, chosen over the deprecated-in-SDK-53/removed-in-SDK-55 `expo-av`) is now the real `SoundService`, with three procedurally synthesized WAV files (`scripts/generate-sound-assets.js`, since no audio tool or licensed sound-asset access existed in the build environment) registered in `skins/lalas-kitchen/soundRegistry.ts` â€” see engine/DECISIONS.md's real-audio-backend entry and `docs/verification/real-audio-backend/`. Still genuinely open from that work, disclosed rather than assumed done: only `match.wav` was live-triggered end-to-end (cascade/win share the identical code path but weren't independently confirmed live); no native device/simulator was available to confirm the real native `AudioPlayer` backend; and no human has listened to the three tones to confirm they read as calm/pleasant, only that real playback is invoked without error. (b) **no distinct `combo_streak` sound** â€” the original ask scoped this to match/cascade/win only; a fourth `SoundEffectId` plus a call site in `Board.tsx`'s `attemptSwap` (alongside the existing `hasCombo` computation) is a small, obvious follow-up if wanted; (c) **no automated `npx expo install`** â€” this environment's npm config rejects `expo install`'s internal `--allow-scripts` flag for project-scoped installs, so `expo-haptics` and (this session) `expo-audio` were installed via a manually-pinned `npm install <pkg>@<bundled-version>` instead (matching `expo/bundledNativeModules.json`'s own pin) â€” revisit if this environment constraint changes.

- Real device build crash (`NoClassDefFoundError` in `expo.modules.asset.AssetModule`) caused by `expo-audio`'s unconstrained `"expo-asset": "*"` peer dependency pulling in a second, SDK-incompatible top-level `expo-asset@57.0.3` alongside the correct nested `~12.0.13` â€” **resolved**: `expo-asset` pinned as an explicit top-level dependency at `~12.0.13` in `package.json`, deduping npm onto the one correct copy â€” see `engine/DECISIONS.md`'s "Fixed: a real device build crash..." entry. Still open, disclosed rather than assumed fixed: no Android SDK/JDK/device exists in this environment, so the actual native build launching cleanly on a real device has not been directly re-confirmed â€” only the dependency-resolution mechanism (`expo-modules-autolinking resolve -p android` now correctly resolving `expo-asset` to `12.0.13`) was verified. Revisit once a real device/build is available to close this out for good.

- ~~Row clearers~~ â€” resolved: the striped piece (a run of exactly 4 spawns a `type: 'striped'` row/column clearer; matching it sweeps that line) is built â€” see engine/DECISIONS.md's striped-piece entry. Remaining special-piece scope still deferred, deliberately, so the special-piece tiers stay bounded and correct: ~~(a) sweep/blast chaining~~ â€” **resolved**: a striped piece, color bomb, or area bomb caught in another's line sweep / detonation / 3Ã—3 blast (or in a combo's clear, or an in-match striped sweep) now fires its own effect too, recursively to fixpoint â€” see engine/DECISIONS.md's special-piece-chaining entry and `docs/verification/special-piece-chaining/`. Built as one shared `expandChainClears` (engine/gameState.ts) wired into both `resolveClearSet` and `resolveMatchEffects`; a caught color bomb (no swap partner) detonates the board's most-common color. ~~One presentation nicety from that work is deferred: the **per-link animation flash**~~ â€” **resolved** (1.0.1 release pass): each chain link now visibly fires one calm beat (260ms) after the link that caught it â€” the engine still folds the chain into a single committed clear set, but `expandChainClears` now records the wave order its BFS already resolved in (`ApplyMoveResult.chainWaveByPieceId`, presentation-only metadata) and the animation layer staggers each wave's cells by it, preserving each link's own travel identity. See `engine/DECISIONS.md`'s 1.0.1 release-pass entry and `docs/verification/chain-staging/`. (b) **L/T-shape triggers** â€” overlapping straight runs are each handled independently (a 4-run's anchor cell wins if another run also wanted to clear it), with no combined/wrapped special for the intersection; a color bomb only spawns from a *straight* 5, and an **area bomb** only spawns from a *pure 2Ã—2 square* (four cells no run touches), never from an L/T intersection of a run and a square meeting at shared cells â€” the built area-bomb detection deliberately stands the square down whenever its cells overlap a run, leaving the run logic to own them (see engine/DECISIONS.md's area-bomb entry). A genuine two-arm crossing (both arms exactly 3) is now a separate, additive trigger â€” see engine/DECISIONS.md's crossing-run entry â€” but an L/T intersection worth a striped piece or color bomb (i.e. one involving a 4- or 5-long arm) remains fully deferred exactly as described above: that intersection stands down at the shared cell and the existing run logic (striped/color-bomb spawn) fires unaffected. ~~(c) bomb + striped combos~~ â€” **resolved**: swapping a striped piece with a color bomb now converts every piece of the striped piece's matchType into a striped piece and fires them all (the super-combo), and swapping two striped pieces clears a full cross â€” see engine/DECISIONS.md's special-piece combos entry and `docs/verification/special-piece-combos/`. ~~One presentation nicety from that work was deferred: the convert-to-striped flash~~ â€” **resolved**: the supercombo now plays a real two-beat presentation identity (a brief "converting" flicker on every converted piece, then a synchronized pop-and-shrink together, rather than the engine's single computed clear rendering as one flat instant) â€” see engine/DECISIONS.md's special-effect-identity entry and `docs/verification/special-effect-identity/`. This is presentation only; the engine still computes the settled clear directly, with no intermediate "everything is now visibly a striped piece" board state â€” only the animation now shows two beats where the underlying data is one. (Blocker clearing was built earlier â€” see engine/DECISIONS.md's Phase 6 section.)
- ~~5+-match specials (color bomb)~~ â€” resolved: a run of **exactly 5** now spawns a `type: 'color_bomb'` piece; swapping it with any piece detonates every piece on the board sharing that piece's matchType (swapping two bombs clears the whole board). It activates on the swap itself, a genuinely different mechanism from the striped piece's match-triggered activation â€” see engine/DECISIONS.md's color-bomb entry, and `docs/verification/color-bomb/` for the live capture. A run of **6+** still clears normally with no larger special; the L/T-shape *trigger* for a color bomb (intersecting runs meeting at a shared point, rather than one straight run of length 5) is deferred alongside the L/T combos in the entry above (the newly-built crossing-run trigger only ever spawns an area bomb from two exactly-3 arms; it never upgrades a 5-long arm's own color-bomb spawn).
- ~~2Ã—2-square specials (area bomb)~~ â€” resolved: a **2Ã—2 square** of four same-type pieces spawns a `type: 'area_bomb'` piece; **swapping it with any ordinary piece** fires the full **3Ã—3** block around it. It's now a **colorless, swap-activated** special (drops its matchType), the same camp as the color bomb â€” this **reverses** its original passive/colored design (matching a colored bomb into a run), because the single universal `area_bomb.webp` sprite gave a player no way to see which color a passive bomb was; making it colorless makes the ambiguity moot. See engine/DECISIONS.md's area-bomb reversal entry and `docs/verification/area-bomb/active/` for the swap-triggered live capture. A 2Ã—2 is a new match *shape* (no 3-in-a-row), so a `checkSquares` scan (`matrix.ts`) runs alongside `checkMatches` and every "is there a match?" gate consults both (`applyMove` snap-back, `resolveCascades` loop, `hasLegalMoves`, generator/`shuffle` guarantees) â€” otherwise a square-forming swap snaps back and latent squares spawn free bombs. ~~Still deferred: the L/T-shape trigger~~ â€” partially resolved: a genuine two-arm crossing-run (a horizontal and a vertical run, each EXACTLY length 3, sharing one cell â€” the classic L/T/plus) now spawns an area bomb at the shared cell too (`matrix.ts`'s `checkCrossShapes`, additive to `checkSquares`) â€” see engine/DECISIONS.md's crossing-run entry and `docs/verification/crossing-shape/`. ~~Still deferred, narrower than before: a **square** overlapping a run (not two runs crossing) still stands down exactly as before~~ â€” **partially resolved**: real playtesting reported a genuine 2Ã—3-rectangle-missing-one-corner shape (a plain exactly-3 run plus a 2-cell extension into the adjacent row/column) not spawning a bomb â€” investigated and confirmed `checkSquares` (`matrix.ts`) already finds this embedded square unconditionally (no isolation requirement at all); the gap was `resolveMatchEffects` (`engine/gameState.ts`) standing down *every* square touching a run indiscriminately. Fixed with `isUnambiguousEmbeddedSquare`: a square overlapping a run now fires anyway when (1) every run touching its cells is exactly length 3, (2) no *other* detected square shares a cell with it, and (3) it shares no cell with a detected cross. See `engine/DECISIONS.md`'s "Embedded square in a straight run" entry and `docs/verification/embedded-square-in-run/`. **Still genuinely deferred, not silently resolved by this fix**: a *genuinely ambiguous* region with two or more overlapping embedded-square candidates (e.g. a full, aligned 2Ã—3 rectangle where *both* rows are independently exactly-3 runs) â€” condition (2) above explicitly detects this and stands both candidates down rather than guessing which one wins; which one *should* win is an open design question, not yet decided. An intersection involving a 4- or 5-long arm also still stands down entirely at that cell, leaving the existing striped/color-bomb run logic to fire unchanged. (Blast chaining â€” an area bomb caught in another effect's clear firing its own 3Ã—3 â€” is now **built**, see entry (a) above.) ~~A square with a live striped corner used to silently fail to form at all~~ â€” **resolved**: `checkSquares` now accepts a live striped corner (blocker/void still excluded), and it fires its own sweep instead of a new area bomb spawning â€” see engine/DECISIONS.md's "Squares: a live striped corner now counts" entry and `docs/verification/square-striped-corner/`.
- ~~area + special combos~~ â€” **resolved**: all three remaining area-bomb pairings now fire a real combined effect instead of snapping back. **area + color bomb** converts every piece of the board's most-common matchType (`mostCommonMatchType`, the same helper a chained partner-less color bomb already used) into an area bomb and fires them all at once â€” several 3Ã—3 blasts simultaneously. **area + striped** fires a plus-shaped blast: the area bomb's 3Ã—3 block unioned with the striped piece's full sweep line, built entirely from the existing `areaBlastPositions`/`sweepLinePositions` helpers. **area + area** fires a single bigger 5Ã—5 blast (not two separate 3Ã—3s) â€” `areaBlastPositions` gained an optional `radius` parameter (default 1, every existing call site unaffected) rather than a second geometry function. All three route through the existing shared `resolveClearSet`, exactly like every other special effect, and `hasLegalMoves` now treats any area-bomb pairing as legal. See engine/DECISIONS.md's "Area-bomb combos: the last three pairings" entry and `docs/verification/area-bomb-combos/`. **Two follow-ups from the later swap-anchor fix** (see engine/DECISIONS.md's swap-anchor entry and `docs/verification/swap-anchor/`): the "area + striped fires a plus-shaped blast" claim above was **not actually true as originally built** â€” both halves anchored on their own separate pre-swap cells, so the sweep line ran down the *edge* of the 3Ã—3 block rather than through its middle (an asymmetric offset union, not a plus); it is a genuinely centred plus now that both halves anchor on the cell the player dragged into. Still deferred: none of the three area+special combos has its own `SpecialEffectDescriptor`, so they animate with the **generic sweep** rather than a distinct visual identity the way `color_bomb`/`striped_cross`/`supercombo` do (`components/specialEffectAnimation.ts`'s module comment had also gone stale, still describing these three as deferred snap-backs that never reach `animateCascade` â€” corrected in the same pass).
- Hidden-piece-underneath blocker mechanic (a blocker conceals a real matchable piece; clearing the blocker layer reveals it, and the revealed piece has to be matched separately) â€” considered as an alternative to the adjacent-damage mechanic actually built for cling wraps (engine/DECISIONS.md, Phase 6), and rejected for now: it doubles the per-cell data model (a blocker cell would need its own state *and* a full hidden `Piece`) for a payoff â€” a second, meaningfully different blocker *feel* â€” that doesn't matter yet with only one blocker type in the skin. Worth revisiting once a second blocker type is real and "every blocker behaves identically" starts to feel thin.
- Recipe box meta layer UI â€” out of scope for V1 per CLAUDE.md; engine's end-of-level summary event should exist eventually but nothing listens to it yet.
- Cloud asset delivery / per-skin CDN loading â€” irrelevant until a second skin is real.
- ~~Multi-target objectives~~ â€” resolved: `GameState`/`LevelConfig`'s `objective` became `objectives`, an array (a single-objective level is length one, no special case); win requires every entry met (`engine/gameState.ts`'s `applyMove`); generated levels place a second objective past a difficulty threshold once the level's own piece-type pool has room for a distinct second target (`appPersistence.ts`'s `generatedObjectiveCount`). See `engine/DECISIONS.md` for the full reasoning. ~~Score-threshold objectives (a numeric score, distinct from counting matched pieces) remain out of scope~~ â€” resolved: see the scoring-system entry below.
- App Store "distinct product" layout variation â€” matters only once skin number two ships.
- ~~What actually decrements `GameState.lives` during a level attempt~~ â€” resolved: losing a level (moves exhausted) now spends exactly one account-level life, applied in `App.tsx` (not mid-level in the engine) and gated at every level-start entry point (`canStartLevel`). The mid-level `pauseReason: 'lives'`/`grantBonusLife` mechanics Phase 4 built for the *other* candidate design (a mid-level lives resource with its own spend trigger) were removed outright rather than kept as unreachable insurance â€” see `engine/DECISIONS.md`'s Phase 7 section for the full reasoning on why that design was dropped instead of finished.
- ~~Basic lives regen has no visible countdown anywhere~~ â€” resolved: `OutOfLives.tsx` now ticks a real "Next life in M:SS" countdown (`formatCountdown`), computed fresh once a second from `appPersistence.ts`'s `msUntilNextLifeRegen` against the same `lives`/`livesLastRegenAt`/`regenMinutes` inputs `applyLivesRegen` already trusts â€” not a separately-tracked value.
- ~~No screen shows the player's current lives count outside of an active level's HUD~~ â€” **resolved** (confirmed already built during the 1.0.1 release pass, this entry had gone stale): `Home.tsx` takes the live reactive `lives` prop from App.tsx and renders it as a calm corner badge (`LivesBadge`) in the hero's top-right â€” exactly the mockup's flame-count spot the entry below described. Live-verified during the 1.0.1 chain-staging capture.
- ~~Per-step cascade animation~~ â€” resolved: `applyMove` now returns `steps: Board[]` (one settled board per cascade pass, last entry equals `state.board`), and `Board.tsx` walks them so each pass animates as its own beat. See engine/DECISIONS.md's cascade-steps entry.
- Literal edge-to-edge fill on all four sides of the board â€” with square tiles and an 8-row/6-col grid on a typical tall phone screen, tile size is bound by screen *width* (6 columns is the tighter constraint), so there's unavoidable vertical slack even at max tile size. `Board.tsx` now centers the grid in the full available height (see `components/NOTES.md`) rather than dumping all the slack below it, but it isn't literally edge-to-edge top and bottom unless tiles go non-square or the grid's row/col ratio is changed â€” neither of which was asked for.
- ~~Dashboard is a dead end once every level in `LEVEL_QUEUE` is complete~~ â€” resolved: past the hand-built queue, `buildLevelConfig` (`App.tsx`) falls through to `buildGeneratedLevelConfig` (`appPersistence.ts`) indefinitely instead of dead-ending, and any hand-built or previously-completed generated level can be replayed via `handlePlayLevel`. Superseded by the Home/All Levels rebuild below â€” the flat `Dashboard.tsx` this note originally described no longer exists; `resolveStartScreen` no longer "always resumes gameplay directly" either (see that entry).
- `LEVEL_QUEUE` in `App.tsx` is still a hardcoded 3-level array (seed/movesLimit/objective per level), not a real level-authoring pipeline â€” adding a 4th *curated* level or rebalancing an existing one still means hand-editing that array. No longer blocking on content running out, though: past index 3, levels are generator-driven and unbounded.
- Generated-level difficulty ramp (`generatedPieceTypeCount`/`generatedMovesLimit`/`generatedTargetCount` in `appPersistence.ts`) is a simple linear step-down with hand-picked floors (3 piece types, 18 moves, and a new 10-piece target floor) and a capped target (26) â€” not tuned against real playtesting, just enough to keep a long session from becoming either trivial or mechanically unwinnable. `generatedMovesLimit`/`generatedTargetCount` now also scale proportionally to a shape template's real playable-cell ratio (see engine/DECISIONS.md's "generated-level difficulty ramp was blind to shape-template playable area" entry, and the board-shape entry below) â€” a real playtest-reported gap, now closed â€” but the underlying step sizes/floors/cap themselves are still hand-picked, not playtested. Revisit once there's actual play data past level ~10.
- `components/AllLevels.tsx`'s row list (formerly `Dashboard.tsx`) grows by one for every generated level the player finishes and never prunes â€” fine at this project's actual scale (one real player, no infinite-scroll concern), but not a paginated/virtualized list if that stops being true.
- Power-up tray UI â€” the empty `powerUpTray` strip (`Board.tsx` + `skinConfig.powerUpTray` + `config.json`) reserved space beneath the board for icons that were never designed or built, and was pulled entirely rather than left rendering nothing. Removing it lets `boardArea` reclaim that fixed height as (larger) centered slack above/below the board â€” the same pre-existing vertical-slack tradeoff already logged above, not a new issue. Real power-up UI is out of scope until actual power-up pieces/behaviors are designed (see the row-clearer/blocker entry above); rebuilding the tray at that point should size itself off real content instead of a placeholder height. ~~A free, non-purchasable "fresh board" affordance~~ â€” **resolved**: a free, uncapped "ðŸ”€ Shuffle" button (`Board.tsx`'s `handleRequestShuffle`, `engine/gameState.ts`'s `requestManualShuffle`) reuses the existing stuck-board `shuffle()` rescue for a genuinely free reshuffle with no move/life cost â€” see `engine/DECISIONS.md`'s shuffle-button entry and `docs/verification/manual-shuffle/`.
- The recipe book's "total" (Home's `{completed} of {total}` and All Levels' enumerable row list) is defined as `resolveVisibleLevelIndices` â€” the hand-built queue's 3 levels always, plus any generated level actually completed (`components/levelProgress.ts`). This was a deliberate call, not an oversight: the generator is unbounded (see `resolveNextLevelIndex`'s own note), so there's no real fixed content ceiling to lock against the way the design mockup's illustrative "24" implied â€” inventing a round number would have been exactly the kind of fabricated value this session was told not to hardcode. The real consequence: once a player is past the hand-built queue and current on generated levels, the progress dot row and "X of Y" always read as fully caught up (a completed generated level is only ever added to the list already-completed) â€” "locked" is only ever observable within the original 3 hand-built levels. Revisit if a real fixed level-count content plan ever exists.
- All Levels renders only two row states (completed/checkmark/tappable vs. locked/dimmed/not-tappable), per this session's written spec. The Claude Design interactive prototype (`skins/lalas-kitchen/design-reference/interactive-prototype.html`) actually shows a third "up next" state â€” the very next unplayed level gets a highlighted border and an "up next" pill instead of being locked like everything else not yet completed. Deliberately not built this session since the written requirements enumerated exactly two states; the immediate next level is still fully playable, just via Home's "Start cooking" rather than by tapping its row on All Levels. Worth a quick confirm if the three-state look is actually wanted.
~~Home's hero banner fade is a dependency-free approximation of the design mockup's actual CSS gradient fade under the hero image~~ â€” **resolved**: `expo-linear-gradient` is now installed, and `Home.tsx`'s hero fade is a real `LinearGradient` spanning the full hero height with stops matching the mockup's actual CSS (`rgba(bg,0)` to 55%, `rgba(bg,0.85)` at 85%, solid at 100%), replacing the old 5-band opacity-stack approximation entirely. See `engine/DECISIONS.md`'s font-and-gradient-polish entry and `docs/verification/font-and-gradient-polish/`. (`components/GinghamTrim.tsx`'s checkerboard trim remains a separate, still-gradient-free approximation â€” a genuine 2-row offset checkerboard, not touched this session.)
~~The design mockups use two custom Google Fonts ('Baloo 2' for headings, 'Nunito Sans' for body) â€” neither is loaded in this app~~ â€” **resolved**: `expo-font`, `@expo-google-fonts/baloo-2`, and `@expo-google-fonts/nunito-sans` are now installed (`components/fonts.ts`'s `useAppFonts`/`Fonts`), and every heading/body style in `Home.tsx`/`LevelMap.tsx` (the current Level Map, `AllLevels.tsx`'s successor) sets a real `fontFamily` instead of relying on the system default. Existing `fontSize`/`fontWeight` numbers were left as-is (already documented above as matching the mockup's choices) â€” only the font family was standing in for the real thing. See `engine/DECISIONS.md`'s font-and-gradient-polish entry and `docs/verification/font-and-gradient-polish/`.
- ~~The mockup's Home hero shows an optional lives/flame-count indicator overlaid on the top-right corner of the header image~~ â€” **resolved** (see the lives-count entry above): built as `LivesBadge` in exactly that hero corner spot, fed by App.tsx's live `lives` value.
- Dynamic denial-zone spread â€” the mechanic itself is built (a gated blocker zone that grows into an adjacent ordinary cell if left unaddressed, with a calm crack/dimming warning the move before â€” see engine/DECISIONS.md's Phase 8 and `docs/verification/denial-zone-spread/`). Deliberately bounded, deferrals logged rather than built: (a) **spread eats only ordinary cells** â€” it never converts a player-earned special (striped/color bomb/area bomb) into a blocker, and never double-places onto an existing blocker; letting a zone consume or interact with specials is a follow-up; (b) **no spread chaining/merging beyond becoming one more ordinary blocker** â€” a spread blocker takes normal adjacent damage like any other; zones don't accelerate, merge, or trigger cascades as they grow; ~~(c) clustered generation is not built~~ â€” **resolved**: `engine/generator.ts`'s `clusteredPositions` (gated behind a new `GeneratorConfig.clusterBlockers` flag, translated from `LevelConfig.denialSpread` in `engine/gameState.ts`'s `createGameState`) grows one contiguous region via random adjacent-cell growth instead of scattering blockers independently, applied only to denial-zone-eligible levels (`generatedLevelNumber >= DENIAL_SPREAD_MIN_LEVEL_NUMBER`) â€” every level below the threshold, and every hand-built level, keeps the original scatter placement untouched. See `engine/DECISIONS.md`'s clustered-denial-zone-placement entry and `docs/verification/clustered-denial-zone-placement/`. `SPREAD_MOVE_FRACTION` (0.25) and `DENIAL_SPREAD_MIN_LEVEL_NUMBER` (10) are still hand-picked, not playtested â€” revisit alongside the generated-level difficulty ramp once there's real play data past level ~10.
- Board shape / void cells â€” non-rectangular boards are now **built** (a `type: 'void'` sentinel piece makes cells non-playable; segmented per-column gravity treats a void as a fixed floor; matching/legal-moves/shuffle/generator/render are all void-aware; `LevelConfig.voidCells` carves the shape; the plus-shaped Level 4 "Cutting Board" showcases it â€” see engine/DECISIONS.md's board-shape entry and `docs/verification/board-shape/`). Deliberately bounded, deferrals logged rather than built: ~~(a) generator-driven shapes~~ â€” **resolved**: `engine/boardShapes.ts` now has a small curated set of 3 reusable templates (`cut_corners`, `plus`, `ring`, each a pure `(rows, cols) => Position[]`), and `appPersistence.ts`'s `generatedShapeId(levelNumber)` gates their introduction the same way `BLOCKER_MIN_LEVEL_NUMBER`/`DENIAL_SPREAD_MIN_LEVEL_NUMBER` gate their own features â€” no shape below generated level number 8, then a 1-in-4 cadence rotating through the 3 templates, wired into `buildGeneratedLevelConfig`'s `voidCells` output. `generateLevel` itself needed zero changes â€” the void-cell contract was already fully general (blockers structurally excluded from void positions, shuffle holds voids fixed, match/legal-move gates already void-aware). See engine/DECISIONS.md's "Generator-driven board shapes" entry and `docs/verification/generator-driven-board-shapes/`. **The template set was later expanded from 3 to 6** (`diamond`/`hourglass`/`pockets`, appended to `BOARD_SHAPE_ROTATION`) â€” a direct architect request for "more board depth," confirming the low-overhead claim above held (each new template was one pure function + two registry lines, zero changes anywhere else) â€” see engine/DECISIONS.md's board-shape-variety-expansion entry. Deliberately narrow within that expansion, not built: (i) **no new hand-built showcase level** for any of the 3 â€” an explicit architect choice (generator-only), not an oversight, since `LEVEL_QUEUE` insertion carries real index-position risk against `completedLevels`/`levelStars`; (ii) **not verified live** â€” confirmed only by the full test suite (657/657), same standing gap as the original 3 templates; (iii) each new template's own taper depth/lattice spacing is a first hand-picked geometric judgment call, not playtested for how it actually feels on a real board. ~~(b) voids Ã— specials interaction~~ â€” **resolved**: a real playtest found that a sweep/blast/detonation reaching a void did **not**, in fact, simply skip it â€” five clear-set builders (the in-match striped sweep, chain expansion, the area-bomb blast, the color-bomb whole-board detonation, and the striped-combo clear-set helper) were written before voids existed and only ever excluded `'blocker'`, so on a shaped board they could clear straight through a void, which segmented gravity then permanently refilled as an ordinary gap. Fixed with one shared `isClearable` predicate at all five sites â€” see engine/DECISIONS.md's "Fixed: the ... claim above didn't hold" entry and `docs/verification/void-specials-clear-fix/`. Void Ã— **denial-spread** interaction remains correctly excluded and un-exposed to this bug (the spread already only ever targets `'normal'` cells, never a void) â€” there's no special that *creates*, *removes*, or *moves* a void, and no shape that changes mid-level. (c) **the "board always full" invariant assumes reachable pockets** â€” segmented gravity refills each enclosed playable pocket from its own local top (a piece appears mid-board), which is correct and calm but means a shape author can't rely on refills *flowing* between pockets separated by a void; a diagonal/flow gravity model was explicitly rejected as overkill (see the DECISIONS entry). ~~(d) the difficulty ramp never accounted for a shape template's reduced playable area~~ â€” **resolved**: a real playtest report on a `ring`-shaped level (55% playable at the real 8x5 generated-board size â€” the most restrictive of the 3 templates) confirmed `generatedMovesLimit`/`generatedTargetCount` computed their numbers from `levelNumber` alone, with no awareness that a shape had just removed cells; both now scale proportionally to `engine/boardShapes.ts`'s new `playableCellRatio(rows, cols, voidCells)`, floored the same way their unscaled versions already were â€” see engine/DECISIONS.md's "generated-level difficulty ramp was blind to shape-template playable area" entry.
- Ad/monetization: the platform-selected `AdService` abstraction (`services/adService.ts` â€” `requestRewardedAd()`/`requestBannerAd()`/`isRewardedAdAvailable()`, selected between an AdMob adapter and a CrazyGames adapter by `Platform.OS`) is now **built**, but real SDK integration is not â€” both adapters are stubs that instantly resolve the reward, matching the two "watch a video" flows' pre-abstraction behavior (see engine/DECISIONS.md's ad-service entry). CrazyGames' "Basic Launch" gap â€” the platform disables all monetization until it reviews and graduates a game to "Full Launch," with no SDK call to detect this proactively â€” is also now **built**: `crazyGamesAdService.ts`'s `CRAZY_GAMES_MONETIZATION_ENABLED` build-time flag (manually flipped, since no runtime check exists) gates both the grant (free when disabled, matching mobile's real-ad path when enabled) and the button copy on `ContinueOffer`/`OutOfLives` (the grant CTA moved from `PausedOverlay` to the new `ContinueOffer` in the mid-level-continue session â€” see `engine/DECISIONS.md`'s mid-level-continue entry) via the new `isRewardedAdAvailable()` â€” see engine/DECISIONS.md's CrazyGames Basic Launch entry and `docs/verification/crazygames-basic-launch/`. ~~Deferred: (a) wiring in either real SDK (AdMob, CrazyGames)~~ â€” **both halves are now resolved**: `services/expoGoogleMobileAdsService.ts` wires in the real `react-native-google-mobile-ads` SDK (a real loadâ†’showâ†’listen cycle for `requestRewardedAd`), using Google's own publicly documented demo App/ad-unit ids (confirmed via direct investigation, not memory) â€” see `engine/DECISIONS.md`'s real-admob-sdk entry. **Not verified live**: this is a native module requiring a real device/dev-client build, which doesn't exist in this environment â€” the same standing gap every other native-only feature here carries. `services/crazyGamesAdService.ts`'s Full Launch path now wires in the real CrazyGames SDK v3 (a browser `<script>` tag, not a native module â€” genuinely testable with an injected fake, and confirmed to have a real local-testing mode on localhost requiring no registered game listing) â€” see `engine/DECISIONS.md`'s real-crazygames-sdk entry. **Verified up to, but not including, the final in-app script execution**: the button/gate logic was confirmed live; the actual script injection-and-run was blocked by this environment's own safety guardrail (unauthorized external code execution) and that block was respected, not worked around â€” a narrower, different gap than AdMob's "no native device" one. ~~(b) `adService.ts` eagerly statically imported both stub adapters~~ â€” **resolved, and a real instance of the risk this note predicted was found and fixed**: `selectAdService` now takes its mobile/web services as injected params (mirroring `hapticsService.ts`'s own `selectHapticsService`). The Metro-resolved `adService.native.ts`/`adService.web.ts` split this note floated as a hypothetical fallback turned out to be genuinely necessary, not avoidable: wiring in the real AdMob SDK broke the entire web bundle (`react-native-google-mobile-ads` transitively imports a native-only codegen component Metro can't bundle for web), caught live while testing the CrazyGames work, fixed with `services/defaultAdService.native.ts`/`defaultAdService.web.ts`. (c) **`requestBannerAd()` has no current caller** â€” still true: built into the interface, but no banner-ad UI exists in the game yet, so both real adapters keep it an honest no-op stub rather than loading a real ad nothing displays. (d) **no automatic detection of Full Launch graduation** â€” CrazyGames genuinely has no SDK-level signal for this today (confirmed by direct doc investigation, not assumed), so `CRAZY_GAMES_MONETIZATION_ENABLED` will need a real developer to flip it by hand whenever CrazyGames actually notifies of graduation; worth revisiting only if CrazyGames ships a proactive check later. (e) **CrazyGames' own recommendation to mute/pause gameplay while an ad plays isn't built** â€” every current caller only ever requests an ad from an already-paused overlay, so the practical gap is narrow, but it's a real, disclosed scope line. (f) **`react-native-google-mobile-ads` is pinned to `16.0.0`, not the latest published `16.4.0`** (see `engine/DECISIONS.md`'s "First real Android build attempt fails on a Kotlin metadata mismatch" entry) â€” a real Android build proved every `play-services-ads` release new enough for `16.1.0`+'s own wrapper code (`setAgeRestrictedTreatment`, added in play-services-ads 25.3.0) is already compiled with Kotlin metadata newer than react-native 0.81.5's own bundled `@react-native/gradle-plugin` can read, and that ceiling can't be worked around from this project's own config (it's hardcoded in a vendored `node_modules` file, and the Kotlin Gradle Plugin only supports one effective compiler version per build). This means age-restricted-treatment consent support (and anything else in 16.1.0â€“16.4.0) is unavailable until either react-native's own bundled tooling bumps its Kotlin past 2.3.0, or a future react-native-google-mobile-ads release adds a way to opt out of whatever forced its own play-services-ads floor up to 25.3.0+. Revisit by re-attempting the same real-build verification whenever react-native or react-native-google-mobile-ads next gets upgraded.
- Web export subpath paths: `scripts/fix-web-export-paths.js` (run via `npm run export:web`) is now **built and verified live** â€” a real `expo export -p web`'s root-absolute paths (`/_expo/...`, `/assets/...`) 404 when the export is mounted under a subpath (e.g. CrazyGames' per-game folder), and the script rewrites them to relative paths post-export; confirmed against a real export served from a real subpath one level below server root, driven by a real browser over CDP, with zero failed requests for anything the app loads â€” see engine/DECISIONS.md's web-export-subpath-paths entry and `docs/verification/web-export-subpath-paths/`. Deliberately narrow, deferred rather than built: (a) **it's a manual post-export step**, not wired into any CI/deploy pipeline â€” nothing automatically runs `npm run export:web` (vs. plain `expo export`) before a real CrazyGames upload, so a developer has to remember to use the right script; worth automating once a real deploy pipeline exists (there isn't one yet â€” this is still a local, unpublished project); (b) **only handles the one root-absolute-path failure mode** actually found in a real export (script src, sprite asset URLs) â€” if a future Expo/Metro version emits some other subpath-unsafe construct (e.g. an absolute `import()` chunk URL), the regex won't catch a shape it doesn't already match; the context-based match (quote + leading slash + word char) was chosen specifically so it keeps working across *asset-layout* changes, but a genuinely new URL *shape* would need a fresh look, not an assumption that this script covers it forever.

- Distinct animation identities for the color bomb detonation, the cross combo, and the supercombo are **built** this session â€” see engine/DECISIONS.md's special-effect-identity entry and `docs/verification/special-effect-identity/`. Deliberately narrow, deferred rather than built: (a) **a color bomb reached via chaining** (caught in another effect's clear, per the special-piece-chaining entry above, rather than the bomb the player actually swapped) still gets the flat generic clear â€” the new radial ripple only applies to the swap's own origin, since a chained bomb has no swap position to radiate from (it detonates the board's most-common color from wherever it happened to be sitting); giving that case its own identity (e.g. radiating from the chained bomb's own resting position) is a real, scoped follow-up, not attempted this session; ~~(b) **the per-link chaining flash remains separately deferred**~~ â€” **resolved** (1.0.1 release pass, see the now-resolved per-link entry above and `docs/verification/chain-staging/`); (c) **no distinct identity for a caught-via-chaining area bomb or striped piece inside a color-bomb/cross/supercombo pass** â€” those already correctly fire their OWN real effect (a chained striped piece's real sweep, a chained area bomb's real powder burst) via the existing per-piece derivations, so they were already fine and untouched, but they don't get any NEW visual tie-in to the outer effect that caught them (e.g. no "this was caught by the wave" cue) â€” cosmetic, not attempted.
- `chain_reaction` tutorial (the fourth one-time card, teaching the moment 2+ specials fire together â€” see engine/DECISIONS.md's `chain_reaction` entry) is **built**, but three things around it are deliberately narrow: (a) **one generic card for both triggers** â€” the copy doesn't distinguish "this was a chain reaction" from "this was a combo," even though `multiSpecialFired` can't tell them apart today either (it's a pure count, not a cause); splitting the copy by cause would need the count threaded alongside *which* mechanism produced it, not just that 2+ specials fired; ~~(b) no dedicated icon art, and no zero-code path to add it later~~ â€” **resolved**: `chain_reaction` (and `how_to_play`, `board_shape` â€” the same structural bind, all three fixed together) now resolve to a real fixed glyph (`spriteAsset.ts`'s `CHAIN_REACTION_TUTORIAL_SPRITE`/`HOW_TO_PLAY_TUTORIAL_SPRITE`/`BOARD_SHAPE_TUTORIAL_SPRITE`) instead of falling through to `spriteLabel`'s generic 2-letter placeholder ("CH"/"HO"/"BO") â€” a real playtest report caught the placeholder text still showing on the board-shape card. `SpecialTutorialOverlay.tsx`'s `FIXED_GLYPH_TUTORIAL_SPRITE` map (renamed from `OBJECTIVE_TUTORIAL_SPRITE`, now covering all 6 no-piece tutorials, not just the 3 objective ones) is the small code change this note predicted would be needed â€” not a registry entry, since there's still no real piece to derive art from. Verified live over CDP against the real running dev server: a real save-state-driven load of "Cutting Board" (the first void-shaped level) rendered the â—± glyph in place of the old "BO" text, and the other two glyphs (âœ³, â‡„) were confirmed to render as real characters (not tofu/missing-glyph boxes) in the same page context â€” see `docs/verification/tutorial-icon-glyphs/`. (c) **same-move collision with a freshly-spawned per-piece tutorial always resolves in chain_reaction's favor** â€” if a single move both mints a not-yet-seen special AND is a genuine multi-special moment, chain_reaction wins (it's set first, synchronously, in `Board.tsx`'s `animateCascade`) and the per-piece tutorial simply waits for a later qualifying move; this was a judgment call (celebrating the moment tied to the animation that just played), not something confirmed against real playtesting since the scenario is rare and hasn't been observed live yet.

- Calm stuck-player hint (`engine/matrix.ts`'s `findAnyLegalMove`, `components/Board.tsx`'s hint button/`hintPair`/`hintUsesUsed`, `components/Tile.tsx`'s `HintGlowOverlay`, `components/pauseActions.ts`'s `canUseHint`/`HINT_USES_PER_ATTEMPT` â€” see engine/DECISIONS.md's stuck-player-hint entry) is **built**: a real, always-visible "ðŸ’¡ Hint" button (no more automatic idle timer â€” that approach was tried at 8s, then retuned to 18s, then abandoned entirely once feedback showed no fixed threshold avoids sometimes interrupting genuine thinking) glows a real legal move on tap, capped at 2 uses per level attempt via the same per-attempt cap mechanism the bonus-moves grant uses. Deliberately not built: (a) **no persisted "seen enough, stop showing" throttle** â€” unlike sound's off-default, there's no real user-research signal yet that this specific player would want the button hidden after using it a lot; if real play surfaces that, a `SaveData` flag is the natural follow-up, not guessed at now; (b) **the 2-use-per-attempt cap is a hand-picked number, matched to the bonus-moves grant's own cap rather than independently tuned or made configurable** per-skin/per-difficulty â€” the two caps happen to share a value today but are tracked by fully independent counters, so either could change without touching the other; (c) **no distinction in the hint's visual treatment by which special (if any) the legal move would involve** â€” a hinted swap that would fire a color bomb, an ordinary 3-match, and a square-forming swap all glow identically; teaching "this specific move does something extra" was never asked for and would risk reading as a tip rather than a calm nudge.

- Level map (`components/LevelMap.tsx`, replacing the old scrollable `AllLevels.tsx`, plus persisted best-ever star ratings in `SaveData.levelStars` â€” see engine/DECISIONS.md's level-map entry) is **built**. ~~(a) no `react-native-svg` dependency â€” the winding path is straight rotated-View segments between node centers~~ â€” **resolved**: `react-native-svg` is now installed and `components/levelMapLayout.ts`'s `computeLevelMapCurveSegments` draws a real Catmull-Rom curve through every node â€” see `engine/DECISIONS.md`'s level-map-curve entry, including a real web-bundle break this caught live (fixed via `LevelMapPath.native.tsx`/`.web.tsx`, the same platform-split pattern the AdMob SDK already needed). The web fallback approximates the curve with many short straight Views rather than a true SVG path â€” good enough to read as smoothly curved, not literally identical geometry to native. ~~(b) no ambient garden decor~~ â€” **resolved**: 6 real landmark illustrations (a start gate, a recipe-box waypoint, 4 rotating journey landmarks) were wired in during the recipe-card art session â€” see `engine/DECISIONS.md`'s recipe-card-and-level-map-art entry; (c) **no ingredient icon or level display name on a node** â€” the approved mockup shows only the level number plus its status decoration; adding either back is a small, obvious follow-up if the map ever feels too anonymous; (d) **no theme/dark-mode system** â€” confirmed none exists anywhere in this app; the map is single-theme (matching every other screen) by explicit architect decision, with the dark variant from the design-approval session kept only as a future reference, not built as a byproduct here; (e) **`MAP_LOCKED_LOOKAHEAD` (4) is a fixed, hand-picked preview depth**, not tuned against real playtesting or made configurable.

- `board_shape`/`spread_warning` tutorials (the sixth and seventh one-time cards â€” see engine/DECISIONS.md's "Two more one-time tutorials" entry) are **built** and fully verified live. ~~One narrow, structural gap logged rather than glossed over: `board_shape` has no dedicated icon art, and â€” unlike the three per-piece tutorials â€” never can.~~ â€” **resolved**: see the `chain_reaction` entry above â€” `board_shape` now shows a real fixed glyph (â—±) instead of the "BO" placeholder, fixed together with `chain_reaction`/`how_to_play` since all three share the same "no single piece to anchor an icon to" structural bind. `spread_warning` never had this gap â€” its icon is the real warned piece's own sprite, the same "show the real thing" convention the blocker tutorial uses. Nothing else about either tutorial is deferred; both are complete against this session's brief.

- Scoring system / `score`-type objectives (`ObjectiveType`'s `'score'` variant, `SCORE_TIER_POINTS`/`passScoreMultiplier` in `engine/gameState.ts` â€” see engine/DECISIONS.md's scoring-system entry) are **built**: every cleared cell scores at one of three tiers (ordinary/special/bomb) depending on the mechanism that cleared it, cascades/chains score at an increasing per-pass multiplier, and a `'score'` objective tracks cumulative move-score instead of one matchType's count. One hand-built level (App.tsx's `LEVEL_QUEUE` â€” "Score Rush", level 5) uses it; verified live over CDP â€” see `docs/verification/score-objective/`. Deliberately narrow, deferred rather than built: (a) **the point values and cascade-bonus rate are hand-picked, not playtested** â€” `SCORE_TIER_POINTS` (10/25/50) and `CASCADE_CHAIN_BONUS_PER_PASS` (0.25) are a first real judgment call, wide enough to feel different on the HUD but not validated against actual play; revisit once there's real data on how fast a score target should feel reachable; ~~(b) `buildGeneratedLevelConfig` (`appPersistence.ts`) never produces a `'score'` objective~~ â€” **resolved**: `isScoreObjectiveLevel`/`generatedScoreTarget` (`appPersistence.ts`) now place one past a gate+cadence (levelNumber >= 3, every 3rd), calibrated against Score Rush's own 1000/24 density, only ever replacing a level's single objective (never mixed with a second `'collect'` target) â€” see `engine/DECISIONS.md`'s generator-driven-score-objectives entry and `docs/verification/generator-score-objectives/`. The gate/cadence numbers and point density remain hand-picked, not playtested. (c) **blockers cleared by adjacent damage always score at the ordinary tier**, regardless of what triggered the clear (a striped sweep's own adjacent blocker damage scores the same as a plain match's) â€” a real simplification, not an oversight, since a blocker never forms a run/special itself; (d) **no distinct scoring identity for chained vs. combo vs. solo detonations beyond their shared tier** â€” a striped+striped cross, a striped+bomb supercombo, and a solo color bomb all score at the same flat 'bomb' tier per cell; giving combos their own even-higher tier was considered and explicitly not done (see the DECISIONS.md entry) to keep the tier count at three, not four-plus.

- Clearance layers / `'clearance'`-type objectives (`ObjectiveType`'s `'clearance'` variant, `GameState.layerCells`, `decrementLayers` in `engine/gameState.ts` â€” see engine/DECISIONS.md's clearance-layers entry) are **built**: a hidden per-cell layer count (1 or 2, the level author's choice) decrements whenever the piece sitting on that cell is cleared by any effect, winning once every layered cell reaches 0. One hand-built level (App.tsx's `LEVEL_QUEUE` â€” "Dusty Counter", level 6) uses it; verified live over CDP â€” see `docs/verification/clearance-layers/`. Deliberately narrow, deferred rather than built: ~~(a) `buildGeneratedLevelConfig` never produces `layerCells` or a `'clearance'` objective~~ â€” **resolved**: `isClearanceObjectiveLevel`/`generatedLayerCells` (`appPersistence.ts`) now place one past a gate+cadence (levelNumber >= 5, every 4th), density-matched to Dusty Counter's own ratio and scaled to the real playable-cell count on a shaped board â€” see `engine/DECISIONS.md`'s generator-driven-clearance-objectives entry and `docs/verification/generator-clearance-objectives/`. This resolved (b)'s "blocker+layer coexistence" question in the SIMPLEST possible direction, not the richest one: a clearance-gated generated level gets NO blockers at all (`blockerCount` forced to 0), sidestepping the harder problem of placing layers around wherever `generateLevel`'s own seeded RNG happens to put blockers (genuinely unknown ahead of time, unlike `voidCells`, which are pure functions of rows/cols known in advance). A real per-cell-aware placement (layers avoiding specific blocker positions on a level that still has blockers elsewhere) remains open if ever wanted. (b) **a layered cell never coexists with a blocker or a void on the same cell** â€” a real, confirmed judgment call (not silently assumed): a layered cell is exclusively an ordinary/special matchable piece this session. Blocker+layer coexistence (does a blocker's own final adjacent-damage clear also decrement the layer underneath it, or only a direct force-clear?) is a genuinely separate design question, deliberately not decided here; (c) **the layer count range (1-2) and `LAYER_OPACITY_STEP`'s wash opacity are hand-picked, not playtested** â€” matches the existing blocker-hits convention for a comfortable default, but revisit once there's real play data. (d) **the generator's own score/clearance cadence-coincidence tiebreak (score always wins when both gates land on the same levelNumber) is a hand-picked resolution, not a deliberately balanced design** â€” the two cadences were never staggered to avoid overlapping in the first place, a real, acknowledged tradeoff rather than an oversight.
- ~~Background-music loop is not wired into `soundRegistry.ts`/any playback service~~ â€” was resolved, **then removed again**: the loop was wired in (`musicRegistry`, `expoAudioSoundService.ts`'s `playMusic`/`stopMusic`, `components/backgroundMusic.ts`'s `syncBackgroundMusic`), gated by the same `soundEnabled` toggle as the one-shot cues and scoped to gameplay only â€” but was later taken out of active gameplay entirely (all of that wiring deleted outright, the same discipline the automatic hint timer got), keeping match/cascade/win completely untouched. `background.wav` itself was deliberately left on disk, unreferenced, as real reusable work should the decision reverse again. See `engine/DECISIONS.md`'s background-music-loop and background-music-loop-removal entries, and `docs/verification/background-music-loop-removal/`. The two previously-disclosed gaps (no human has listened to the loop; no native device test) are now moot â€” there's no loop left to listen to or test.

- The tutorial-cadence throttle (`appPersistence.ts`'s `TUTORIAL_MIN_GAP_MS`/`canShowTutorialNow`/`shouldActivateTutorial`, `components/Board.tsx`'s `activeTutorialId`/`nextEligibleTutorialId` â€” see engine/DECISIONS.md's tutorial-cadence-throttle entry) is **built**: a real 60-second minimum gap between any two of the seven one-time tutorials actually appearing, so two genuine firsts (e.g. a shaped, blockered generated level's `board_shape`+`blocker`, or forming a striped piece then a color bomb on consecutive moves) defer instead of stacking, rechecked on every real move commit rather than a background timer. Verified live, including a real gap the capture itself found and fixed (a deferred mount-time tutorial's cooldown clearing on the exact move that wins/loses the level used to risk popping over the Won/Paused overlay â€” now gated on `gameState.status === 'in_progress'`) â€” see `docs/verification/tutorial-cadence-throttle/`. Deliberately narrow, deferred rather than built: (a) **a still-pending mount-time tutorial (`board_shape`/`blocker`) whose level ends (win or loss) before its cooldown clears is never shown for that specific level** â€” it isn't lost forever, since `shouldShowBoardShapeTutorial`/`shouldShowBlockerTutorial` re-derive fresh from scratch at the next level's own mount and will show then if that level also triggers the condition, but the player doesn't get the explanation anchored to the exact board that first surfaced it; a real fix (e.g. carrying a genuine "still owed" queue across level boundaries) wasn't asked for and adds real state-lifetime complexity for a narrow edge case. (b) **`TUTORIAL_MIN_GAP_MS` (60 seconds) is a hand-picked judgment call, not playtested** â€” reasoned from the existing 18s single-thinking-pause idle-hint research (a tutorial-to-tutorial gap should read as noticeably longer than one pause), but no real player data confirms 60s specifically feels right rather than, say, 45 or 90.
- First accessibility pass (font-scale cap + `AppText.tsx` wrapper across all 16 `Text`-importing components, `allowFontScaling={false}` on icon-glyph/fixed-diameter elements, a real CVD-simulation check of the six ingredient + three special sprites â€” see `engine/DECISIONS.md`'s "First accessibility pass" entry) is **built**. Deliberately narrow, deferred rather than built: (a) **no real Android/iOS device or emulator confirms the actual OS-level text-size setting** â€” this session's `?fontScaleDebug=N` in-tree simulation faithfully exercises the real component styles (react-native-web's `PixelRatio.getFontScale()` always returns 1, so there's no browser-native way to test this), but a genuine device/simulator test is the same standing disclosed gap every other native-only feature in this project carries; (b) **`WonOverlay`/`PausedOverlay`/`ContinueOffer`/`OutOfLives` were confirmed clip-safe by file-by-file code audit (no fixed-height `Text` containers exist in any of them) rather than independently screenshotted at 2x** â€” a real screenshot only proves one specific copy length anyway, so the structural audit is arguably the stronger claim, but it wasn't paired with a live capture the way Home/board-HUD/LevelMap were; (c) **only `accessibilityLabel` was added or confirmed â€” no `accessibilityRole`/`accessibilityHint` audit was done**, since the brief scoped this pass to font-scale and colorblind-safety specifically, not a full screen-reader pass; a genuine VoiceOver/TalkBack pass (roles, hints, focus order, live regions for HUD count changes) is real, separate follow-up work; (d) **no additional non-color visual cue was added to any piece** â€” the CVD simulation found the existing sprite shapes already sufficient, so nothing was added; revisit only if real colorblind playtesting ever disagrees with the simulation.

- iOS device-family restriction (`app.json`'s `ios.supportsTablet: false` + the new `ios.requireFullScreen: true` â€” see `engine/DECISIONS.md`'s iOS-device-family entry) is **built**, but with two real, disclosed gaps rather than a claimed-complete fix: (a) **the actual generated `Info.plist`/`project.pbxproj` was never directly inspected** â€” `expo prebuild --platform ios` refused to run on this Windows environment ("Run npx expo prebuild again from macOS or Linux"), so verification stopped at `npx expo config --json` confirming the resolved config, one step short of the real native project Xcode/EAS Build would actually compile; revisit from a real macOS/Linux build to close this out for good. (b) **`requireFullScreen: true`'s actual interaction with iPadOS 26's "Windowed Apps" floating-window mode for iPhone-only apps is unconfirmed** â€” no documentation found this session settles whether it blocks that specific newer windowing mode the way it blocks classic Slide Over/Split View; it was added as a reasonable, architect-confirmed hedge, not a verified fix. Separately, and more fundamentally: **no app.json/Info.plist-level configuration can make this app genuinely uninstallable on iPad** â€” `TARGETED_DEVICE_FAMILY = 1` only means "not iPad-optimized," and Apple's own guidelines (2.4.1) explicitly still let iPhone-only apps run on iPad via a virtualized, upscaled compatibility window, which is almost certainly the actual mechanism behind the App Store rejection this session responded to. The one documented way to truly block iPad installation (declaring an iPhone-exclusive `UIRequiredDeviceCapabilities` like `telephony`) was deliberately not implemented, since this app has no telephony dependency and declaring one would misrepresent its capabilities to Apple.

- App icon (`assets/icon.png` wired into `app.json`'s `icon`/`ios.icon`/`android.adaptiveIcon.foregroundImage`+`backgroundColor`/`web.favicon`, plus the checked-in `android/app/src/main/res/mipmap-*` files replaced directly â€” see `engine/DECISIONS.md`'s app-icon-recovery entry) is **built**, recovered from an orphaned local EAS build (`7d846df0...`, unreachable both locally and on GitHub) rather than recreated: the IPA/APK for that build were downloaded before their listed artifact expiration and real shipped icon images extracted directly (Apple's loose CgBI-optimized bundle-root PNGs, hand-decoded; the Android APK's real per-density `ic_launcher*.webp` originals, no re-derivation), and the true 1024Ã—1024 master (unrecoverable from `Assets.car`'s compiled/compressed format with tools available in this environment) was separately supplied by the user from the same original source and verified by direct IHDR inspection plus a visual match against the extracted art before being wired in. Deliberately narrow, disclosed rather than smoothed over: (a) **`android.adaptiveIcon.foregroundImage` reuses the same flattened 1024 master** â€” the original build's config referenced a distinct `adaptive-icon-foreground.png` (presumably a safe-zone-padded, transparent-background layer), but no pixels for that specific file were ever recovered from either build artifact; an OEM launcher's circular/squircle adaptive mask will crop into the flattened image's outer margin exactly as it would for any icon with no inset, a real but minor cosmetic gap; (b) **this only matters for a future iOS rebuild** â€” Android builds in this project have never run `expo prebuild` (the `android/` folder is hand-maintained and committed as-is), so `app.json`'s `adaptiveIcon` field is inert for today's Android builds; the actual fix there was the direct native mipmap replacement; (c) **no true CAR-format (`Assets.car`) parser exists in this toolchain** â€” a raw byte scan for embedded PNG signatures found zero, confirming CoreUI's compiled catalog uses Apple's own structured/LZFSE-style internal format; recovering a *higher*-resolution iOS asset directly from a future compiled catalog (rather than from loose bundle-root files or a separately supplied master) would need a real macOS-side tool (`assetutil`/`actool`) this environment doesn't have.

- Defensive save loading, an app-level `ErrorBoundary`, and a hardened `shuffle` rescue fallback (a real playability-audit's two flagged-critical findings plus one closely related gap found investigating them) are **built** â€” see engine/DECISIONS.md's "Three real-playability-audit safety fixes" entry and `docs/verification/safety-hardening/`. Deliberately narrow, deferred rather than built: ~~(a) no crash-reporting/telemetry service~~ â€” **partially resolved**: `ErrorBoundary.tsx`'s `componentDidCatch` still logs via `console.error`, but now also persists the crash into the real save (`SaveData.lastCrash`, `engine/gameState.ts`'s `recordCrash`), calmly surfaced on `components/Settings.tsx` â€” a real, checkable signal with no new dependency or external account, architect-chosen over a remote service (Sentry or similar) given this project's actual scale. Still genuinely open: no PROACTIVE/remote signal exists â€” someone has to physically open Settings on the device to ever see it. See `engine/DECISIONS.md`'s in-app-crash-log entry and `docs/verification/crash-telemetry/`. (b) **no native (iOS/Android) device test** for any of the three fixes â€” same standing disclosed gap as every other feature in this project, no attached device/simulator exists in this environment. (c) **no live click-through of actual gameplay exercising the modified `applyMove`/`shuffle` code path** â€” a CDP synthetic-mouse-event attempt against React Native Web's gesture-handler-backed `Pressable` didn't register a real swap; confidence instead rests on the engine test suite (521 tests, including a real adversarial ring-board scenario), which is the right tool for engine-logic correctness â€” worth a fresh live attempt (e.g. real touch-event dispatch instead of mouse events) if this specific gap ever needs closing. (d) **`forceLegalMove`'s two templates (in-line 4-window, three-plus-off-axis-notch) cover every board shape this project currently ships** (rectangles, `cut_corners`, `plus`, `ring` â€” confirmed live against the real, most-restrictive `ring` case) but aren't a mathematical proof of coverage for every conceivable future shape template; a hypothetical shape with neither a long straight run nor any perpendicular neighbor anywhere could still hit `shuffle`'s final throw â€” revisit if a 4th board-shape template is ever added to `engine/boardShapes.ts`.

- The objective-tutorial trio (`score_objective`/`clearance_objective`/`escort_objective` â€” see `engine/DECISIONS.md`'s objective-tutorial-trio entry) is **built**: a one-time card explaining each non-`'collect'` objective type, closing a real gap found by a general playability review (every other new mechanic already had one; these three never did). Deliberately narrow, deferred rather than built: (a) **the copy is a first real judgment call, not confirmed against actual playtest feedback** â€” several other tutorials' wording was retuned after a real report (the stuck-player hint's idle threshold, the automatic-timer approach it eventually replaced entirely), and this trio hasn't had that chance yet; (b) **not verified live** â€” no browser/device session ran the session that built this, the same standing gap every other feature here discloses without one; confidence rests on the test suite (638/638) and direct code tracing, not a real render; ~~(c) the curated `LEVEL_QUEUE`'s own pacing is untouched~~ â€” **resolved**: confirmed with the architect that the real (and only) existing player's save is already ~322 generated levels past the hand-built queue, so reordering/inserting hand-built levels can no longer touch anything she's already completed â€” the progress-corruption risk this entry originally raised no longer applies. Three plain breather levels ("Spoon Stir," "Herb Snip," "Lemon Drizzle") were inserted into `App.tsx`'s `LEVEL_QUEUE`, one directly before each genuine "first" past level 3 (board shape, score, clearance, escort), so no two new ideas ever land back-to-back for a new player. See `engine/DECISIONS.md`'s queue-breather-levels entry. Still open: the exact moves/target numbers on the three new levels are hand-picked, not playtested, same as every other hand-built level's own numbers.

- Generated-ramp continuity (the piece-type pool no longer resets to 3 right after the hand-built queue ends â€” see `engine/DECISIONS.md`'s generated-ramp-continuity entry) and generated-level naming (see the generated-level-naming entry) are both **built**, closing two of six findings from an external playability review (a third, deleting the dead `getWonSummary` helper, is pure cleanup with no deferred surface). Both verified live over CDP against the real running dev server. ~~The station-name pool (14 entries) ... nobody has confirmed the names read well~~ â€” **the 14-entry flat list itself was replaced**, not just left disclosed as hand-picked: a real architect concern (a 14-name cycle repeats ~23 times by the real player's actual level, 330) prompted swapping it for two coprime-sized word pools (13 Ã— 17 = 221 combinations before an exact repeat) â€” see `engine/DECISIONS.md`'s revision note on the generated-level-naming entry. Still open: (a) **the 30 words across both pools, and the ramp-continuity mechanism itself, are both hand-picked, not playtested** â€” nobody has confirmed the names read well in practice or that continuing the ramp at 6 types (rather than some gentler intermediate value) feels right, and 221 levels between repeats is a large improvement but still not literal infinity; (b) **three other findings from the same review were deliberately NOT actioned this session** â€” the hint cap's *direction* (uncap vs. raise vs. degrade-gracefully) needs an architect decision, not a guess; the recipe book's blank locked cells are a documented, deliberate non-gamified design choice that a fix would have to consciously reverse, not just patch; and OutOfLives' countdown-vs-"No timers"-tagline tension bundled a real new feature (a non-timed practice mode) into what read like a copy fix â€” see `engine/DECISIONS.md`'s "Three findings from an external playability review" entry for the reasoning behind each.

- Real recipes behind each card (tap an unlocked `RecipeBook.tsx` card to open `RecipeDetailModal.tsx`, showing real ingredients/steps â€” see `engine/DECISIONS.md`'s "Real recipes behind each card" entry) were originally **built** for the existing 12 cards only, as an explicit proof of concept. ~~(a) the 40 additional cards ... will land with no `recipe` content~~ â€” **resolved**: see the 40-card-extension-wired entry below; all 52 cards now carry real `recipe` content. Still deferred: (b) **`RecipeCardReveal.tsx`'s one-time win-overlay reveal is still not tappable** â€” only the persistent `RecipeBook.tsx` collection screen opens the detail view; making the reveal moment itself tappable too remains an obvious, cheap follow-up; (c) ~~all 52 recipes are a first-draft, unverified pass~~ â€” **partially resolved**: the 40 new recipes got a real correctness pass (see `engine/DECISIONS.md`'s recipe-correctness-pass entry) checking proportions, leavening, missing prep steps, and food-safety wording â€” 8 real issues found and fixed. Still genuinely unverified: nothing in this project's 52 recipes has actually been cooked by a human and confirmed to taste right or turn out as described â€” the pass caught structural/technical defects (a cake with no leavening, bread with no salt), not "does this actually taste good"; the original 12 have not had the same structural pass; (e) **no dedicated test file exists** for `RecipeBook.tsx`/`RecipeDetailModal.tsx` â€” this project has no test harness for mounted React components (same standing gap as `Home.tsx`/`LevelMap.tsx`), so live verification over CDP is this feature's only verification.

- The `lemon_squeeze`/`classic_tomato_soup` milestone mismatch is **resolved** (`lemon_squeeze` now unlocks at level 2, matching the level it's actually named after) â€” see `engine/DECISIONS.md`'s "milestone mismatch is resolved" entry. ~~A 40-card milestone extension curve was designed alongside it ... but is not yet wired into config.json~~ â€” **resolved**: see the 40-card-extension-wired entry below; the full 40-number curve (50 through 625) is now live in `config.json`, paired 1:1 with the 40 cards from the `recipe-card-watercolor-pack-2026-07-29-expansion` art pack in that pack's own manifest order. **A real, disclosed consequence now actually landed, not just anticipated**: 25 of the 40 new milestones fall at or below the real player's current level (330 as of this note), so all 25 will backfill into her save silently on her next load via the existing `backfillUnlockedRecipeCards` path, with none of them getting the normal one-at-a-time `RecipeCardReveal` celebration â€” the mechanism is confirmed correct (it's exactly what it was built for), this is just a larger-than-usual instance of it, worth knowing about before her next session rather than being surprised by a "25 of 52 recipes collected" jump. Still open: (a) **the 40-cards-to-40-milestones pairing used the art pack's own manifest order as a default**, not an architect-confirmed sequence â€” cheap to re-pair later since `findRecipeCardForLevel` does a plain `.find()` scan, unaffected by array order; (b) **6 further "alternate" card illustrations from the same pack** (`recipe-card-pack.manifest.json`'s `alternateCards` â€” `basil_summer_pasta`, `lemon_butter_beans`, `garlic_herb_noodles`, `rustic_chili_pasta`, `creamy_herb_potatoes`, `tomato_herb_bake`) were deliberately left unwired â€” no milestone slots exist for them in the current curve, and inventing new ones wasn't asked for; the raw PNGs remain in the untracked `_assets/` staging folder if ever wanted.

- No pre-submission version-bump guard exists. `app.json`'s `version` (the App Store `CFBundleShortVersionString`) has to be manually bumped to something strictly higher than the last *approved* App Store version before every submission, or Apple rejects it outright (`ITMS-90186`/`ITMS-90062` â€” see `engine/DECISIONS.md`'s "App Store bumped the app to a closed pre-release train" entry, which is exactly this happening for real on build 11). Nothing in this project's workflow checks this ahead of time â€” `eas.json`'s `autoIncrement` only covers the native build number, a separate counter Apple doesn't apply this rule to. Deferred rather than automated: a real fix would mean querying App Store Connect's actual last-approved version before every `eas submit` (an API call this project's tooling doesn't make anywhere yet) â€” reasonable for a solo-maintainer project at this cadence to just remember by hand, revisit if this bites twice.

- OTA updates (EAS Update, `expo-updates`) are **wired in** â€” `app.json`'s `runtimeVersion.policy: "fingerprint"`/`updates.url` and `eas.json`'s per-profile `channel` fields â€” see `engine/DECISIONS.md`'s ota-updates-wired entry. Genuinely deferred, not silently assumed working: (a) **the currently-live App Store build (1.0.1) cannot receive an update at all** â€” it was compiled before `expo-updates` existed in this project, and only a build compiled *after* this session gains the OTA path; the next submission is still a normal full build+review cycle regardless; (b) **no real `eas update` has ever been published from this project** â€” there's no build yet that could receive one, so the actual publishâ†’fetchâ†’apply round-trip is unverified end-to-end, confirmed only by config resolution (`npx expo config --json`) and a clean web-bundle recheck, not a live update; (c) **no in-app "what's new"/update-available surfacing exists** â€” deliberately, matching this game's calm/no-timers brief (the default silent-check-then-apply-next-launch behavior needed zero app code), but it also means there's no way for a player to know an update just landed, which could matter later if OTA is ever used for something more visible than a recipe-card batch.

- Special-piece **anchor** work (where an effect fires, and where a new special is born) is built â€” see `engine/DECISIONS.md`'s swap-anchor and spawn-anchor entries, `docs/verification/swap-anchor/` and `docs/verification/spawn-anchor/`. Deliberately left undone, disclosed rather than silently settled: (a) the three **area+special combos have no `SpecialEffectDescriptor`** of their own, so they animate with the generic sweep rather than a distinct visual identity the way `color_bomb`/`striped_cross`/`supercombo` do â€” the swap-anchor work corrected their *geometry* but not their *look*; (b) `runCentreIndex`'s **"earlier of the two middles"** tiebreak for an even-length cascade-formed run is a hand-picked judgment call, not playtested â€” a 4-run has no true centre and the later middle would be equally defensible; (c) a **2Ã—2 square formed by a cascade keeps its top-left anchor**, since a square genuinely has no centre cell to prefer instead â€” so the one shape that can still spawn at a scan-order corner is a cascade-formed square, which is much rarer than the run case the report was actually about; (d) the swap-anchor and spawn-anchor fixes were each **only partly driven live** (the solo area bomb and striped cross for swap-anchor, the 4-run for spawn-anchor) â€” the remaining shapes rest on real-`applyMove` probes plus unit tests, and none of it has been confirmed on a native device or played by a human.

- Pre-submission playability pass (see `engine/DECISIONS.md`'s playability-pass entry) â€” three real gameplay problems fixed, with these limits disclosed rather than smoothed over. (a) **The greedy solver built to measure difficulty is NOT a valid instrument for `'escort'` levels** â€” established by control, not guessed: it loses the shipped, live-verified hand-built "Delivery Day" 3 times out of 3, because one-move lookahead can't plan the sustained digging-under-one-column that escorting needs (even with a descent gradient added to its scoring). Generated escort difficulty (2 dropdowns growing to 3, 24-move floor) is therefore anchored on that hand-built precedent rather than measured, and is the one lever in the pass without evidence behind it. Its results for `'collect'` levels remain sound â€” it wins those consistently, which is what the star and level-1 conclusions rest on. (b) **No human has played any of this.** The star retune (3 stars now at half the budget unused), level 1's loosening (target 15 -> 12, moves 20 -> 24) and the score/clearance cadence widening are all calibrated against a bot, which is a far better instrument than arithmetic but still not a person â€” and this game is built for one specific real player whose actual pace nobody has measured. (c) **The generated difficulty ramp still flatlines**: `movesLimit` floors and `targetCount` caps by roughly generated level 25, after which the only things that vary are which board shape alternates in, which blocker type rotates, and which of the four objective types the cadences pick. Level 300 is numerically identical to level 60. Not addressed here, and not obviously wrong for a calm endless game, but it is the reason "the game stops changing" past a certain point. **The "which shape/which blocker" half is now resolved** (see `engine/DECISIONS.md`'s "loop-variety, round two" entry) — both rotations moved off predictable-by-level-number arithmetic onto the same shuffle-bag technique objective type already had, a deliberate architect choice ("more variety, not more difficulty") over the alternative of raising the numeric caps themselves, which stays untouched on purpose: this game's calm/not-punishing brief is real user research, and there was no appetite for risking that by making a 300+-level save's difficulty start climbing again. (d) The Android permission removals (`RECORD_AUDIO` and expo-audio's microphone service, plus three stock-template permissions) are validated only as **well-formed XML with a correct fully-qualified service name** â€” the manifest merge itself needs a real Android build to confirm, which this environment can do via the WSL toolchain but did not run this session. expo-audio's media-playback foreground service is deliberately left in place, since whether the library starts it internally could not be verified without a device.

- Game-feel overhaul (distance-proportional fall timing, top-edge spawn streaming with board clipping, content-driven pass scheduling, accelerating fall easing, the ordinary-match anticipation beat â€” see SPEC.md and `engine/DECISIONS.md`'s game-feel-overhaul entry) is **built**, engine untouched. Deliberately narrow, deferred rather than built: (a) **continuous per-column gravity resolution** â€” the genre's true model, where each column resolves locally the instant space opens with no global pass boundaries at all. The in-scope content-driven scheduling keeps the engine's settled-board pass snapshots as the presentation contract; going fully continuous abandons that contract and touches the same structural boundary the board-topology investigation already declined to cross. Revisit only if real play still reads as stepped after this pass. (b) **Enclosed void-segment spawns fade in place** (`planSpawnEntries`' `fadeInPlaceIds`) rather than getting any travel â€” an edge-streamed entry would visibly cross a void; a per-segment "stream from just above the segment" variant was considered and rejected in SPEC.md's fork resolution as visually worse. Rare by construction (only shaped boards with interior pockets, e.g. `plus`). (c) **Every new constant is hand-picked, not playtested**: the three `FALL_SPEED_PROFILES` (base/perCell/cap per speed), `PASS_BEAT_MS` (140), `SPAWN_FADE_MS` (200), and the `MATCH_POP_*` trio (90ms/1.08/0.25). All flow through `cascadeTiming.ts`, so retuning is one file. (d) **Not verified live in-session** â€” the SPEC.md verification table's frame-trace rows (velocity consistency, spawn first-visible-frame, schedule timestamps) and, decisively, the real on-device feel check by a human remain open; per the spec this feature is not done until that field report lands.
- Playability review 2026-08-08 (read-only, see `docs/playability-review-2026-08-08.md`) surfaced one genuinely new engine-level finding and several presentation/tuning gaps the repo had not yet disclosed. Deferred, not built, since the review's deliverable was the review itself:
    (a) **a generated level past generated levelNumber ~12 can combine a 4-blocker board with a `sealed_jar` (specialOnly) blocker AND active denial-spread** - the three gates (`generatedBlockerCount` capping at 4 around level 9, `BLOCKER_MIN_LEVEL_NUMBER.sealed_jar` at 12, `DENIAL_SPREAD_MIN_LEVEL_NUMBER` at 10) are independent and co-saturate, and spread-minted blockers inherit `specialOnly`, so a player who hasn't built a special yet can face a growing, ordinary-match-immune wall the difficulty breather does nothing about (it only trims moves/target). Two candidate fixes, neither chosen: gate sealed_jar vs. denial-spread off each other (e.g. a sealed_jar level never spreads), or let the breather also trim blocker count. Worth an architect decision, since it is a real difficulty spike, not a cosmetic one.
    (b) **score is computed by the engine for every move but only ever displayed on 'score'-type levels** - `Hud.tsx` shows only Target/Moves/Lives; surfacing the per-move score + cascade pass multiplier is presentation-only work with zero engine change and is the highest-impact reward-texture lever available. Also the explicit open ground named by the 2026-08-08 "Scope of calm" note.
    (c) **every special effect renders in the single shared `palette.accent`** - per-mechanism effect hues (striped / color bomb / area bomb / combos) would differentiate real mechanical depth the eye currently can't see; same scope note, also open ground.
    (d) **`COMBO_STREAK_THRESHOLD` = 4 is likely too deep for this specific casual player** - a hand-picked one-line tuning change (consider 3) but genuinely a playtest question, not something a code review can settle.
    (e) ~~the no-moves shuffle rescue animates visibly but never says so~~ - **resolved**: `ApplyMoveResult.stuckBoardRescued` now feeds a `LalaMomentBanner` line ("The board needed a little reset.") reusing the existing banner mechanism rather than a new fade pill — see `engine/DECISIONS.md`'s shuffle-notice-and-cap entry.
    (f) **star-rating thresholds are never shown pre-win** - stars are computed from movesRemaining vs movesLimit but the player only learns the outcome after the fact; showing "X moves left for 3 stars" is cheap and gives the persisted best-ever stars a reason to exist.
    (g) **hand-built levels 1-3 use all 6 piece types, contradicting the generator ramp's own "fewer types = gentler" thesis for a brand-new player** - a 3-4 type pool on the first two levels is the gentlest fix; not urgent for the one real player (past level 300) but matters for any fresh install.

- Visual reward language (per-mechanism effect colors, visual-only reward-intensity scaling, the solo area bomb's new radial effect identity — see engine/DECISIONS.md's matching entry) is **built**. Deliberately narrow, deferred rather than built: (a) **the six effect colors' CVD safety is only a scratch-verified preliminary pass** — a real protanopia/deuteranopia simulation was run and the color set revised until it passed (min pairwise distance 32.7/45.0), but unlike the original piece-sprite CVD work (`docs/verification/accessibility-pass/colorblind-simulation/`), no checked-in artifact exists against the actual rendered board — the verification script was scratch, not committed; (b) **the area+special combos (area+color, area+striped, area+area) still fall through to the generic sweep with no distinct color or motion identity of their own** — only the solo area bomb blast was brought into scope this session, matching the spec's own named list; giving the three combos their own identity remains open, same as it was before this session (this entry does not change that prior deferral, only reconfirms it under the new color system); (c) **`passRewardIntensity`'s formula (pass-index and cleared-count weights) is hand-picked, not playtested** — it's a deliberate presentation-only approximation of the engine's real scoring (see the DECISIONS.md entry for why coupling them was rejected), so it can never drift out of sync with the engine, but its own felt calibration (does a 2-cascade genuinely feel bigger than a 1-cascade by the right amount?) is unverified; (d) **not yet seen firing on a real cascade in any session** — the board cannot render in this project's browser-pane environment (the same standing, previously-documented limitation every animation feature here discloses), so the six colors and the intensity scaling are unverified as an actual felt change until a real desktop or on-device playtest, the same closing standard the game-feel overhaul needed.

- HUD reward texture and character (`ApplyMoveResult.score`, `Board.tsx`'s `runningScore`, the redesigned `Hud.tsx` "Kitchen Tray" — see engine/DECISIONS.md's matching entry) is **built**. Deliberately narrow, deferred rather than built: (a) **the per-cascade-pass floating `+N`** (the playability review's own "clearest chains-are-worth-more teach") was explicitly deferred, not rejected — the runner chose a persistent-total-only build to validate whether visibility alone helps before paying for the real per-pass `ApplyMoveResult` breakdown that option needs; revisit once the total-only version has had real play against it; ~~(b) **a small, measured tap-accuracy cost remains on the game's narrower-column levels** — real CDP measurement on level 1 (8×5) found `tileSize` bound by height at 63px vs. a theoretical 65px if width still bound, after two real tightening passes; closing the remaining ~2px/tile would mean visibly undercutting the "not bland" ask itself, so it was accepted as the honest stopping point — a 6-column-or-wider level has no such cost, `byWidth` sits well below `byHeight` regardless of HUD height~~ — **superseded, not resolved**: the 63px baseline itself moved to 60px in the chrome-trim pass below (a different tradeoff — the bottom icon-badge toolbar, not the tray — now owns the cost); the "6-column-or-wider has no such cost" reasoning still holds unchanged; (c) **the two-row multi-objective layout was verified by code review, not a live render** — the live CDP check this session only reached level 1, a single-objective level; a real generated 2-objective level's HUD has not been visually confirmed; (d) **not yet felt-verified at all** — the tray's actual "character" (does it read as warm and alive, not just technically taller and colored) is inherently a human judgment the browser-pane environment's board-render limitation can't substitute for, the same standing gap every visual feature this session discloses. **The score plaque itself is gone now** — see the mascot-medallion entry below; `ApplyMoveResult.score`/the engine's per-move scoring are untouched (still feeds ScorePopup and 'score'-type objectives), only the persistent per-attempt total display and its own `runningScore` state were removed.

- The HUD's character-portrait medallion (`Hud.tsx`'s `mascotFrame`, resolving `mascot.webp` via the existing sprite pipeline) is **built** — a direct architect call to remove the running-score plaque and use its freed slot for a real mascot once codex produced one. Deliberately narrow, deferred rather than built: (a) **no fallback plan if `mascot.webp` is ever regenerated at a very different aspect ratio** — the frame center-crops via `resizeMode="cover"`, tuned against the current art's roughly-6:7 portrait crop; a much wider or taller replacement could crop awkwardly, unverified until it happens; (b) **not felt-verified at all** — same standing board-render-limitation gap as everything else in this file; the frame's size (34px) was tuned only against a real *measured* tileSize cost, not against how the portrait actually looks at that size; (c) **the rest of codex's art pass (updated base/striped/special piece sprites) is untouched by this entry** — those are drop-in replacements at existing filenames needing no code changes, reviewed separately.

- Commercial-polish consolidation (the codex stash — `ScorePopup`, `LalaMomentBanner`, `KitchenSceneDecor`, `WonOverlay`'s `AnimatedStar`, plus a new `WinCelebrationBurst` for a strong win) is **built** — see `SPEC.md`. Deliberately narrow, deferred rather than built:
  - **`lala-moment-banner-coverage`**: `resolveLalaMomentCopy` (`components/rewardMoment.ts`) wires 9 of `release-character-pack.md`'s 12 copy-bank triggers (up from codex's original 4). ~~"Automatic no-moves rescue" is deliberately silent by an earlier, still-valid design decision~~ — **resolved, same session**: real feedback reversed that decision (see `engine/DECISIONS.md`'s shuffle-notice-and-cap entry), and `ApplyMoveResult.stuckBoardRescued` now feeds this trigger too. The remaining 3 are still a considered decision, not an oversight: "Level win" and "Recipe unlock" are each already carried by a richer, dedicated treatment at that exact moment (`WonOverlay`'s headline/stars/burst; `RecipeCardReveal`) — a text banner underneath would be redundant at best, and likely invisible once the win overlay's backdrop covers the board where the banner renders; "Special piece created" (distinct from "activated," which is wired) would need a new engine signal — detecting a newly-spawned special separately from one merely firing — that doesn't cheaply exist today and risks confusing overlap with the now-wired "activated" line in the same move. Revisit "Level win"/"Recipe unlock" only if a future pass makes those overlays render later/smaller such that the banner underneath would actually be seen.
  - **`WinCelebrationBurst` placement is a single shared anchor (true center), not tuned per branch** — `WonOverlay.tsx`'s `celebrationSlot` centers the burst over whichever of `RecipeCardReveal` or the plain plated-dish illustration is showing, rather than a bespoke anchor point for each (the plate's own visual center vs. the recipe card's glow center differ slightly). Chosen for one simple implementation over pixel-tuned placement per branch; revisit if a real playtest finds the center feels off for either branch specifically.
  - **Not felt-verified at all** — same standing gap every visual/animation feature in this file discloses: the board (and, for a win, the overlay stacked on it) cannot be screenshotted in this project's browser-pane environment, so `AnimatedStar`'s pop, `ScorePopup`/`LalaMomentBanner`'s fade timing, and `WinCelebrationBurst`'s actual felt intensity (does 14 particles over ~560ms read as delightful or as "too much" even gated to a strong win) are all verified by code-level reasoning, prior-convention matching, and the full test suite only — not by a human seeing or feeling them. Real device/desktop playtest is the closing step.
  - **Particle count/timing/colors are hand-picked, not playtested** — `PARTICLE_COUNT` (14), `FLIGHT_MS` (560), and the golden-angle/ring-distance layout in `components/celebrationParticles.ts` are a first reasonable pass reusing this game's existing color language (yolk/flame/accent/secondaryAccent), not tuned against real feedback.

- Loop variety, win-tier rewards, and recipe engagement (SPEC.md's three-thread pass, see `engine/DECISIONS.md`'s matching entry) is **built**. Deliberately narrow, deferred rather than built:
  - ~~Board-shape template rotation and blocker-type selection stay on their original fixed cadences~~ — **resolved**: both now use the same seeded shuffle-bag technique objective type got in this pass, closing the exact gap this bullet named — see `engine/DECISIONS.md`'s "loop-variety, round two" entry.
  - **The new star thresholds (1/3, 1/6) are a disclosed hypothesis, not a fresh calibration** — the original 1/2 bar was itself a bot-simulation correction, but no casual-play telemetry exists for this single-player project to calibrate against, so both bars were halved by reasoned guess (preserving relative tier spacing) rather than re-simulated. Revisit numerically once real sessions land against these.
  - **The shuffle-bag's per-mechanic salt constants, the daily-bonus grant (one shared token, one calendar day, no cap), and the win-celebration intensity numbers (7/380ms/0.6x vs. 14/560ms/1x) are all hand-picked, not playtested** — first reasonable passes, not tuned against real feedback.
  - **The recipe-progress fill bar's actual visual appearance was never seen, only confirmed to render without crashing** — this project's browser-pane environment cannot composite the page for a real screenshot, the same standing gap every visual feature here discloses. Whether the bar's size/color/icon placement actually reads well against the existing card layout is unverified.
  - **The daily-bonus token's announcement banner and the shuffle-bag's real generated-level behavior were never driven live end-to-end** — both require either a real level win or reaching deep into the generated-level sequence, and per this project's standing testing discipline, forcing either condition against the architect's real save data was not attempted this session. Verified by direct code tracing and the test suite only.

- Genre-reference chrome/layout trim (bottom icon-badge toolbar for Exit/Hint/Shuffle, a real top-to-bottom background gradient, the in-play level-name line dropped — see `engine/DECISIONS.md`'s real-photo-playtest-comparison entry) is **built**. Deliberately narrow, deferred rather than built:
  - ~~Chunky/shaded piece-art and a mascot/character portrait are not attempted~~ — **resolved**: codex produced a real art pack (updated piece/special sprites plus a new mascot) in a later session; the mascot is now wired into the HUD and `LalaMomentBanner` — see `engine/DECISIONS.md`'s mascot-wiring and toast-facelift entries. The rest of codex's sprite updates are drop-in replacements needing no code changes.
  - **The bottom toolbar still costs a real, accepted ~5% tileSize reduction (63px→60px at the reference viewport)** even after the two chrome-trim offsets (merging Exit in, dropping the level-name line) — a deliberate, measured tradeoff the architect confirmed rather than a free win. If a future pass wants that back, shrinking the badge size (56px→~44px) is the cheapest lever, estimated to roughly halve the remaining cost, not yet attempted since it wasn't asked for.
  - **Not felt-verified at all** — the actual tile grid still cannot render in this project's browser-pane environment (a pre-existing, unrelated limitation — see [[browser-automation-board-render-limitation]]), so tileSize impact was measured indirectly via the HUD/toolbar chrome (which renders independently) rather than by seeing the real board. Whether the gradient background and badge toolbar actually read as "more polished" rather than just "different" is a human judgment call, unconfirmed by a real device or desktop playtest.
  - **The gradient direction/stops and toolbar badge size/spacing are a first reasonable pass, not tuned against real feedback** — reuses the palette's existing two stops and a plain top-to-bottom wash; no alternative gradient angles or badge sizes were tried.

- Toast facelift (`components/surfaceChrome.ts`'s shared `SURFACE_SHADOW`/`SURFACE_BORDER_WIDTH` — renamed from the toast-only `toastChrome.ts` once the Home-screen pass below needed the same values — applied to `ScorePopup`/`ComboStreakBanner`/`LalaMomentBanner`, plus a mascot avatar on `LalaMomentBanner` specifically — see `engine/DECISIONS.md`'s matching entry) is **built**. Deliberately narrow, deferred rather than built:
  - **Not felt-verified at all** — none of the three toasts could be organically triggered this session (each needs a real match/cascade/shuffle/level-win, and the actual tile grid still doesn't render in this project's browser-pane environment). Whether the shadow/border actually reads as "has depth," and whether the avatar sits well against the banner's own text at 24px, are both unconfirmed by a human.
  - **`ScorePopup`/`ComboStreakBanner` deliberately stayed text-only** — no established character-voice framing for either (unlike `LalaMomentBanner`, which reads from the actual Lala copy bank), so adding an avatar to either would be decoration for its own sake rather than a real content tie-in. Revisit only if a real playtest specifically asks for it.
  - **The shadow/border values are a direct reuse of `Board.tsx`'s existing `toolbarBadge` material**, not independently tuned for a toast's own smaller scale — a first reasonable pass favoring one consistent design language over per-component tuning.

- Home screen facelift, two passes (`components/surfaceChrome.ts` material; a gradient background matching `Board.tsx`; Lala's portrait scaled up into a real 64px hero lockup with the title, not a small avatar beside the quote; the "Up Next" card wearing `skinConfig.palette.tray` instead of a plain panel — see `engine/DECISIONS.md`'s two matching entries, "Home screen facelift" and "Home screen, round two") is **built**. The first pass was real feedback-corrected mid-stream ("hardly even changed anything... disconnected from gameboard direction") into a genuinely structural second pass, not just more shadows — both passes got live DOM verification (Home actually renders in this project's browser-pane environment, unlike the board), including a real WCAG-contrast regression (~3.96:1, short of 4.5:1) caught by direct computation and fixed before shipping, not left in. Deliberately narrow, deferred rather than built:
  - **Not confirmed by a human eye** — DOM-level verification (shadow presence, gradient/tray colors, element position/size, computed contrast, zero console errors) is real evidence the geometry, colors, and contrast are all correct, but not the same as someone actually looking at it. The same standing gap every visual feature here discloses, just with the strongest evidence floor any pass in this project has had.
  - **`Board.tsx`'s own `toolbarBadge` still holds its own separate inline copy of the shadow values**, not migrated to import `surfaceChrome.ts` — a disclosed, not-yet-collapsed duplication, left alone since touching `Board.tsx` wasn't part of either pass's actual scope.
  - **The "Browse all levels" secondary button deliberately stayed flat, with no gradient/tray treatment either** — a real visual-hierarchy call (cards, the hero, and the primary CTA all got bolder; the tertiary action didn't), not an oversight; revisit only if that reads as inconsistent once seen live.
  - **The recipe-book/settings cards deliberately kept the plain panel material**, not the tray — only the literal game-entry-point card got that treatment, a deliberate hierarchy choice (this screen shouldn't read as "everything is now brown wood"), not an inconsistency.
  - **The 64px hero avatar size/border width, and the tray-material choice itself, are a first reasonable pass**, not tuned against how it actually looks at real device scale — the `/design` CLI command the architect originally asked for was never actually run this session (confirmed it can't be invoked via the Skill tool; the architect chose to have this session redesign directly instead), so no second design opinion has weighed in on this direction yet.

- The white-stripe fix on Level Map (`LevelMap.tsx`'s `mapGlowTop`/`mapGlowBottom` overlays removed — see `engine/DECISIONS.md`'s matching entry) is **built**. ~~Not addressed: `LevelMap.tsx`'s own screen background is still the flat `background[0]` fill~~ — **resolved same day**: `LevelMap.tsx` now uses the same `LinearGradient` over `palette.background` as `Board.tsx`/`Home.tsx`, live-verified over CDP (real resolved gradient colors, zero console errors) — see `engine/DECISIONS.md`'s Level-Map-gradient entry. Still open: `RecipeBook.tsx`/`Settings.tsx` are the only two screens left on a flat fill, never raised or audited this session. Revisit as its own scoped pass if asked — same mechanical change, no new palette data needed.

- The grid-width change (8x5 -> 8x7, real report: "the grid feels tiny compared to the genre" — see `engine/DECISIONS.md`'s grid-width entry) is **built**. Deliberately narrow, deferred rather than built:
  - **Only Level 1 "Tomato Toss" was re-verified against the new board size**, via a throwaway greedy-solver check (won 5/5 seeds, 5-11 of 24 moves used). The other 9 non-showcase hand-built levels (Lemon Squeeze, Herb Garden, Spoon Stir, Score Rush, Herb Snip, Dusty Counter, Pantry Corners, Lemon Drizzle, Delivery Day) kept their original `movesLimit`/`targetCount` numbers unverified against the wider board — a bigger board is the safe direction (generally easier, not harder) but this wasn't individually confirmed per level.
  - **The generator's own `MIN_MOVES`/`MIN_TARGET`/ramp floors, `BREATHER_TARGET_RATIO`/`BREATHER_SCORE_RATIO`, and every other hand-picked difficulty constant were left numerically unchanged** — they already scale off `playableRatio`/cell counts computed from the real `rows`/`cols` passed in, so they adapt automatically, but their absolute tuning was calibrated against the 40-cell board and has not been re-felt at 56 cells.
  - **No human has seen the wider board render** — the actual tile grid still doesn't composite/paint in this project's browser-pane automation environment (the same standing, pre-existing limitation, see [[browser-automation-board-render-limitation]]). The claim that 7 columns actually reads as "less tiny" is supported by real geometry (near-edge-to-edge width fill) and math, not by a screenshot or a real device look.
  - **`engine/boardShapes.test.ts`'s and `engine/generator.test.ts`'s test description strings still say "(8x5)"/"the real generated board size (8x5)"** — those tests exercise the board-shape template functions generically at that fixture size, which remains functionally valid, but the parenthetical claim about matching production is now stale. Left unfixed since it's a documentation-accuracy gap, not a test-correctness one — revisit if a future session touches those files anyway.
  - **The respaced `DUSTY_COUNTER_LAYERS`/"Delivery Day" `dropdownPositions` are a first reasonable re-layout**, not re-validated against actual play — they preserve the original row values, layer-count split, and total count, but the specific new column choices were picked by eye to spread across the wider board, not measured or playtested.

- Track A's hit-stop treatment and juiced audio (`cascadeTiming.ts`'s `EXPERIMENTAL_HIT_STOP_MS`, `services/soundService.ts`'s `match_juice`/`cascade_juice`/`special_trigger` ids — see `engine/DECISIONS.md`'s matching entry) are **built**, both opt-in-only through the game-feel-comparison dev harness, never live for a real player. Deliberately narrow, deferred rather than built:
  - **No real on-device/real-audio-device listen** — the same disclosed gap every prior sound-redesign entry in this file carries. The new cues' logic (which id plays, on which pass, layered correctly) was verified via a temporary instrumentation log against the real running app, not by ear; nobody has heard `match_juice`/`cascade_juice`/`special_trigger` yet.
  - **`EXPERIMENTAL_HIT_STOP_MS` (90ms) is a hand-picked judgment call**, not tuned against a real reaction to the freeze — chosen to read as "a deliberate beat" without feeling like a stutter, but never felt live (this environment's browser automation can screenshot the board but has no way to judge a 90ms timing pause by eye).
  - **The decision to gate hit-stop/audio behind the SAME opt-in-only override the particle burst uses** (rather than shipping either as a live default, which SPEC.md's own prose arguably invited) was an auto-mode judgment call, not confirmed with the architect first — flagged in `engine/DECISIONS.md`'s entry rather than guessed at silently. Revisit if the architect wants either to ship as a real production default instead.
